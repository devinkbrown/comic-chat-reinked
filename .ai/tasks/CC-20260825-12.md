# Task contract: CC-20260825-12

## Outcome

PR #13 types Armenian and Georgian without an IME and consumes a desktop
startup activation token so a launched window can take focus. No UI/renderer,
no AT-SPI, no full IME editor, no dedicated drop Event, no Wayland taskbar
urgency, no `_NET_WM_SYNC_REQUEST`, no Wayland→X11 fallback. Both Linux
arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/xkb.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `src/platform/services.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `a5be1c2`

## Evidence and constraints

- Existing behavior: modern Unicode-mapped keysyms already work; named
  Armenian/Georgian XKB symbols and legacy `0x14xx`/`0x15xx` keysyms do
  not. Startup tokens are ignored, so dock/file-manager launches stay
  behind other windows.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; `xdg_activation_v1` is used only to consume a supplied token,
  not as notify/urgency.

## Acceptance criteria

- [ ] Named Armenian/Georgian keysyms type without an IME
- [ ] Legacy X11 Armenian `0x14xx` and Georgian `0x15xx` keysyms type
- [ ] Wayland consumes `XDG_ACTIVATION_TOKEN` via `xdg_activation_v1`
- [ ] X11 sets `_NET_STARTUP_ID` from the desktop startup token
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: keysym tables plus startup-token helper plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
