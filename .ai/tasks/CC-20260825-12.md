# Task contract: CC-20260825-12

## Outcome

Finish leftover Sunday chrome on PR #11 so the remaining 90s shell (focus
order, keyboard completeness, dialog titles, live/offline leftovers, composer
send/action chrome, Cobalt/raw internals copy) reads as Ink Sunday around the
1:1 page.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `src/client/shell.zig` (focus cycle only)
- Read-only dependencies: `src/main.zig` tests that pin stored failure strings; `src/client/geometry.zig`
- Forbidden paths: `src/comic/original_*`, `src/comic/strip.zig`, `src/platform/*`, CAST/bodycam blit geometry, comic-page placement, CI, packaging
- Base commit: `865eadf`

## Evidence and constraints

- Existing behavior: Tab cycle is navigation → toolbar → transcript → composer → say_actions → members → emotion. Status is not in the cycle. Enter chip shows only when composer is empty. Mode clicks currently send. Failure status display still uses raw stored strings. Several dialog titles remain Title Case / LISTX.
- Source/reference evidence: Ink Sunday chrome around the 1:1 page; do not restyle CAST/bodycam crop or comic-page placement.
- Invariants from `docs/WORKERS.md`: no SDL; no second heuristic layout; no golden raster hash refresh without source evidence.

## Acceptance criteria

- [x] Status is in the Tab/Shift-Tab focus cycle with a visible focus ring
- [x] Status keyboard: Enter toggles the panel; when open, Left/Right chooses Connection setup / Settings and Enter activates
- [x] Whisper and Sound say-actions disable when the wire is dead (skip keyboard, refuse activate)
- [x] Composer shows a Send/Enter chip whenever width allows, including when text is present; mode buttons set mode only (Sound still opens its dialog)
- [x] Dialog leftovers (Title Case, LISTX query) use Sunday copy
- [x] Failure status display is Sunday-ized without changing stored `setConnectionFailure` strings
- [x] No visible "Cobalt" chrome; enum `.cobalt` and leftover `"Cobalt"` mapping stay
- [x] Pixel tests that pin stroke, navigation, tabs, composer ink, mood/dial/page well remain green
- [x] `zig fmt`, `zig build test`, `zig build -Dtarget=x86_64-linux`, `git diff --check` pass
- [x] Committed render-ui previews refreshed

## Verification

- Focused gate: `zig build test`
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/client/shell.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `src/client/ui.zig`, committed `ui-*-preview.png`
- Commands and outcomes: `zig fmt` clean; `zig build test` pass; `zig build -Dtarget=x86_64-linux` pass; `git diff --check` pass
- Evidence: focus cycle now ends on status; status Enter/arrows; whisper/sound skip while offline; composer Send chip; Sunday dialog titles; display-only `Wire failed (...) - click Connect`
- Residual risk: protocol/network chrome and CAST/bodycam crop remain other lanes
- Recommendation: `accept`
