# Task contract: CC-20260825-03

## Outcome

Color/HD live pages keep full standing local-color figures. A balloon
taller than dest min-height no longer covers a face when dest can
slide or the Color balloon dest can lift, without moving Microsoft
goldens. Packaged AVBs stay standing. Rooms stay multi-hue.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_layout.zig`,
  `src/comic/original_balloon.zig` (Color-only lift helper),
  `src/comic/strip.zig` (Color-only dest/balloon post-pass),
  this contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `6d05929`

## Evidence and constraints

- Existing behavior: dest nudge slides Color dests below a 3-line
  cloud. When `clear_above >= max_bottom`, dest snaps to
  `max_bottom - min_h` and the face goes back under the balloon.
- Invariants: shipped strip stays on `original_*`; goldens stay.
  Testdata dests do not use `keep_recognizable`.

## Acceptance criteria

- [ ] Color dest never snaps back under a panel-height balloon.
- [ ] Color-only balloon lift leaves testdata goldens unchanged.
- [ ] Wrap / Color chrome stay standing local-color; rooms multi-hue.
- [ ] `zig build test` green. Proof shots: Anna plus two other Color
      characters.

## Verification

- Focused gate: dest-nudge + balloon-lift + wrap/chrome tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Recommendation: `accept`
