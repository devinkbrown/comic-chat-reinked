# Task contract: CC-20260825-03

## Outcome

PR #13 closes another usable-desktop slice: cheap text/file-path DnD,
leftover clipboard MIME, X11 HiDPI scale refresh, and window-state
tracking. No UI/renderer, no AT-SPI, no full IME editor, no
Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `6c7b3ce`

## Evidence and constraints

- Existing behavior: clipboard text MIME is present; drops are ignored;
  X11 scale is read once at open; xdg configure states only refresh IME.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; dropped text is injected as existing key events, not a new
  chrome path.

## Acceptance criteria

- [x] Wayland and X11 accept text / `text/uri-list` drops as typed keys
- [x] Clipboard MIME includes `text/plain;charset=utf8` and `text/uri-list`
- [x] X11 re-reads `Xft.dpi` on root `RESOURCE_MANAGER` changes
- [x] Wayland/X11 track maximized/fullscreen window state
- [x] `zig build test` plus both Linux targets

## Verification

- Focused gate: URI/MIME/scale/state tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes:
  - `zig build test` — 486 pass, 1 skip
  - `zig build -Dtarget=x86_64-linux` — success
  - `zig build -Dtarget=aarch64-linux` — success
  - `zig build linux` — success
  - `zig build -Dtarget=x86_64-windows` — success
  - `git diff --check` — clean
  - `env -u WAYLAND_DISPLAY DISPLAY=:1 timeout 3 ./zig-out/bin/reinked window anna` — exit 124
- Evidence: URI-list/MIME/xdg-state unit tests; X11 smoke stayed up
- Residual risk: XDND TypeList is read only when the more-than-3 flag is
  set; drops become existing key events, not a dedicated Event.drop
- Recommendation: `accept`
