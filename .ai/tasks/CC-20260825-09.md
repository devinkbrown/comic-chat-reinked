# Task contract: CC-20260825-09

## Outcome

PR #13 closes more usable-desktop leftovers: X11 VisibilityNotify expose
coalescing, per-output RANDR HiDPI when the window moves, Hebrew keysyms,
ICCCM MULTIPLE paste, Wayland frame-callback present coalesce, and a
one-shot SSD decoration retry. No UI/renderer, no AT-SPI, no full IME
editor, no Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/xkb.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Read-only dependencies: `src/platform/services.zig`, `src/platform/event.zig`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `8ce2cd5`

## Evidence and constraints

- Existing behavior: Expose is delivered while fully obscured; X11 scale
  ignores per-output mm when the window moves; Hebrew keysyms are `.other`;
  MULTIPLE selection requests fail; Wayland `present()` commits every call;
  decoration configure nacks are ignored.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits; no XIM/AT-SPI.

## Acceptance criteria

- [x] Fully obscured X11 windows skip Expose; becoming visible emits Expose
- [x] Cached RANDR CRTCs refresh integer scale when the window moves
- [x] Hebrew named and X11 keysyms produce characters
- [x] X11 serves ICCCM MULTIPLE atom-pair selection requests
- [x] Wayland present waits for `wl_surface.frame` before the next commit
- [x] Wayland re-requests SSD once if the compositor configures CSD
- [x] `zig build test` plus both Linux targets

## Verification

- Focused gate: visibility / CRTC / Hebrew / MULTIPLE / frame / decoration tests
- Aggregate gate: `zig build test`
- Build gate: `zig build` plus `x86_64-linux` and `aarch64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/xkb.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`, `.ai/tasks/CC-20260825-09.md`
- Commands and outcomes:
  - `zig build test` — success
  - `zig build -Dtarget=x86_64-linux` — success
  - `zig build -Dtarget=aarch64-linux` — success
  - `zig build linux` — success
  - `zig build -Dtarget=x86_64-windows` — success
  - `git diff --check` — clean
  - X11 smoke — exit 124
- Evidence: unit tests for visibility state, CRTC hit-test, Hebrew
  keysyms, MULTIPLE pair layout, frame deferral, and CSD mode
- Residual risk: no full IME editor, no AT-SPI, no Wayland taskbar
  urgency, no `_NET_WM_SYNC_REQUEST`
- Recommendation: `accept`
