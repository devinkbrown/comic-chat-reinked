# Task contract: CC-20260824-01

## Outcome

Make shipped strip rendering 1:1 with Microsoft Comic Chat algorithms
(panel packing, avatar placement/scale, balloon routing/tails, emotion/pose,
AVB/BGB pixels), with remaining visual/behavior gaps listed honestly.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `2`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/` (strip, figure, original_*, source_*_test),
  `src/render/` (canvas/font used by strip), focused docs/notes, render-strip
  previews. Surgical `src/client/view.zig` planner/blit integration only.
- Read-only dependencies: pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684`, `docs/WORKERS.md`
- Forbidden paths: desktop chrome rewrite (`src/client/ui.zig` restyle),
  network/TLS, asset redistribution without provenance
- Base commit: `origin/main`

## Evidence and constraints

- Existing behavior: `original_*` + `strip.zig` is the shipped pipeline;
  `layout.zig` is a leftover heuristic and must stay off the product path.
- Source/reference evidence: microsoft/comic-chat at the README pin.
- Invariants from `docs/WORKERS.md`: no second layout/balloon path; golden
  hash changes require an upstream routine; no SDL; linux/amd64 must build.

## Acceptance criteria

- [x] Simple-avatar `GetDimInfo` keeps source `ydim/2` (`avatar.cpp:63`), not `face.y`.
- [x] Color/HD generated AVBs have strip regression coverage.
- [x] `layout.zig` is fenced as non-shipped.
- [x] Desktop comic buffer feeds the source planner a transcript prefix.
- [x] Empty conversation uses the source title panel.
- [x] Renderer tests pass; linux/amd64 `zig build` succeeds.
- [x] PR lists remaining non-parity honestly and includes render-strip previews.

## Verification

- Focused gate: `zig test -Mroot=src/root.zig --test-filter source`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/figure.zig`, `src/comic/strip.zig`,
  `src/comic/source_strip_test.zig`, `src/comic/layout.zig`,
  `src/client/view.zig`, `src/main.zig`, `.ai/tasks/CC-20260824-01.md`
- Commands and outcomes:
  - `zig build` — linux/amd64 success
  - `zig build test --summary all` — 474 passed, 1 skipped
  - `git diff --check` — clean
  - `reinked render-strip {source,color,empty} png` — 650×1655 / 315×315
- Evidence: microsoft/comic-chat `avatar.cpp:63` (`headHeight = ydim/2`);
  `panel.cpp` AddLine/LayoutAvatars already ported; prefix vs last-nine
  strip test proves the windowed-slice establishing-shot bug.
- Residual risk: Comic Neue metrics, Color DIB vs 1-bit MERGEPAINT halo,
  reserved 1–6 column desktop grid, viewport scale, chrome HD remap.
- Recommendation: `accept`
