# Task contract: CC-20260824-01

## Outcome

Redesign the portable Zig desktop chrome as a new Ink Sunday Comic Chat
product around a 1:1 source-faithful comic page. Stay in `src/client`
UI/shell/dialogs so renderer and Linux PRs can land separately.

## Route

- Score: `3 + 1 + 3 + 2 = 9`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `2`
- Critical domain: `source-parity`

## Ownership

- Owned paths:
  - `src/client/ui.zig`
  - `src/client/view.zig`
  - `src/client/dialogs.zig`
  - `src/main.zig` (window title only)
  - `docs/UI_LIBRARY.md`
  - `packaging/comicchat.desktop`
- Read-only dependencies:
  - `src/comic/original_*.zig`
  - `src/comic/strip.zig`
  - `docs/WORKERS.md`
- Forbidden paths:
  - Qt / SDL / second application
  - Golden raster hash refresh without source evidence
- Base commit: `74b19bd`

## Evidence and constraints

- Existing behavior: Fluent-style chrome, 9-line comic window, empty wireframe.
- Source/reference evidence: Microsoft Comic Chat `c7df00f60bc8e9fdef413f139e61f7c37e024684`.
- Invariants from `docs/WORKERS.md`: no SDL, no second heuristic layout, Onyx TLS pinned, strip path through `original_*`.

## Acceptance criteria

- [x] Desktop chrome is a new visual system, not an in-place Fluent polish.
- [x] Comic pages still render through `strip.renderWithOptions`.
- [ ] Linux `x86_64-linux` and `aarch64-linux` compile (other agents).
- [ ] PR includes before/after `render-ui` previews and leftover list.

## Verification

- Focused gate: `zig build test` plus `render-ui` variants
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux` and `zig build -Dtarget=aarch64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept`
