# Task contract: CC-20260824-08

## Outcome

Finish remaining PR #11 Ink Sunday chrome: persist slash-command
layout, Alt menu accelerators, dialog labels, window title, tab
overflow, empty/error copy. CAST/page placement unchanged.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`,
  `src/client/dialogs.zig`, `src/main.zig` (persist, title, prefill)
- Forbidden paths: CAST/bodycam blit, `src/comic/*`, `src/platform/*`
- Base commit: `8ba91ec`

## Evidence and constraints

- Slash `/view` `/members` change layout without save
- No F10 in Key enum; Alt modifier is available
- Default profile string is source-faithful wire copy — do not change
- CAST blit stays `52×52` at `cell.y + 6`

## Acceptance criteria

- [ ] Slash-command layout writes `ui_*` preferences
- [ ] Alt+letter/down opens the menu bar
- [ ] Dialogs have named primary actions (no leftover OK)
- [ ] Window title matches Sunday masthead
- [ ] Overflow rooms show a tab chip
- [ ] CAST/page placement unchanged
- [ ] Tests green; previews refreshed if pixels change

## Verification

- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`

## Handoff

- Recommendation: `accept`
