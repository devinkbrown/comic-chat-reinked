# Task contract: CC-20260824-08

## Outcome

Live Color client ships a complete Anna: full standing silhouette
(head through heels) in chrome, gallery, CAST, bodycam, and strip
panels, with real local color. Close remaining renderer leftovers
that still block a shippable Color client.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_layout.zig` (dest inset / full
  standing), `src/comic/figure.zig` (crop includes heels),
  `src/comic/original_figure.zig` (if dest/ROP leftovers),
  `src/comic/strip.zig` (tests), `src/client/view.zig` (gallery /
  bodycam contain-fit), this contract.
- Read-only dependencies: pose-sheets-v1, pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684`
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `142936f`

## Evidence and constraints

- Existing behavior: dest sits on the 2300-unit ground line
  (`max_body_height = unit_height/1.9`). The 93×189 card fills dest
  and feet kiss the panel bezel; chrome/bodycam read as mid-thigh.
- Source/reference evidence: testdata zoom goldens require the
  pre-zoom top for unflagged bodies. Color/HD `keep_recognizable`
  dests may inset without moving those goldens.
- Invariants from `docs/WORKERS.md`: shipped strip stays on
  `original_*`; source goldens stay unless an upstream routine
  justifies a change.

## Acceptance criteria

- [x] Anna Color/HD chrome, gallery, CAST, and bodycam contain-fit
      the full standing silhouette including heels.
- [x] Strip / conversation wrap panels show a whole standing Anna,
      not legs-only, waist-cut, or right-clip.
- [x] Local color stays peach / red / black; rooms keep wood, plants,
      sky. No one-hue wash.
- [x] At least one leftover that still blocks a complete Color
      client is closed (dest inset, heel crop, or sticker halo).
- [x] `zig build test` green; source goldens unchanged.
- [x] Previews refreshed on PR #12.

## Verification

- Focused gate: Anna full-silhouette tests + wrap no-legs + goldens
- Aggregate gate: compiled module + main tests
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `original_layout.zig` (standing dest + ground inset),
  `original_figure.zig` (near-white sticker AA is paper), `figure.zig`
  (heel-band tests), `view.zig` / `dialogs.zig` (taller gallery,
  bodycam foot room), Anna v1 poses re-split from the 3×2 sheet,
  Color/HD Anna AVBs rebuilt, checksum manifests.
- Commands and outcomes: `zig build test` 493 pass / 1 skip; Color
  and HD Anna checksums verified.
- Evidence: chrome Anna is 86×253 head-through-heels with peach /
  red / black / red heels. Wrap and conversation panels show the
  same standing figure. Gallery and CAST bodycam contain-fit it.
- Residual risk: balloons still draw after bodies (source order),
  so a large balloon can cover the face. Heels sit near the panel
  bezel by design of `CBodySingle` bottom-align. Packaged wrap
  slivers remain in other characters' DIBs (ignored, not restitched).
  Testdata `anna` stays 1-bit for goldens.
- Recommendation: `accept`
