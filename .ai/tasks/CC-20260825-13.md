# Task contract: CC-20260825-13

## Outcome

X11 finishes startup-notification (remove message after map) and restores
the pointer cursor on EnterNotify. No chrome/renderer, no AT-SPI, no full
IME, no dedicated drop Event, no Wayland taskbar urgency, no
`_NET_WM_SYNC_REQUEST`, no Wayland→X11 fallback. Both Linux arches still
build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `8b13919`

## Evidence and constraints

- Existing behavior: `_NET_STARTUP_ID` is set but the X11 remove
  ClientMessage is not sent, so some desktops keep a busy cursor. The
  window event mask does not include EnterWindow, so a stolen cursor is
  not restored on re-entry.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback.

## Acceptance criteria

- [x] Startup-notification remove payload encodes `remove: ID=` with escaped spaces
- [x] X11 sends `_NET_STARTUP_INFO` remove after MapWindow
- [x] EnterNotify reinstalls the scaled cursor
- [x] `zig build test` plus both Linux targets

## Verification

- Focused gate: remove-message helper plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
