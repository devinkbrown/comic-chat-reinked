# Task contract: CC-20260825-13

## Outcome

Finish remaining leftover 90s shell chrome on PR #11 (Title Case / LISTX /
internal copy, dialog completeness, keyboard leftovers, live/offline status,
composer/action chrome) so the Ink Sunday frame around the 1:1 page no longer
reads as the old client.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `src/client/shell.zig` (focus/keyboard only)
- Read-only dependencies: `src/main.zig` stored failure strings; CAST/bodycam blit; comic-page placement
- Forbidden paths: `src/comic/original_*`, `src/comic/strip.zig`, `src/platform/*`, CAST/bodycam crop, comic-page placement, CI, packaging
- Base commit: `7efa713`

## Evidence and constraints

- Existing behavior: previous pass added status to Tab, Send chip, Sunday-ized many titles, whisper/sound disable, display-only failure labels
- Source/reference evidence: Ink Sunday chrome around the 1:1 page
- Invariants from `docs/WORKERS.md`: no SDL; no second heuristic layout; no golden raster hash refresh without source evidence

## Acceptance criteria

- [x] Remaining Title Case / LISTX / raw-internal chrome copy is Sunday
- [x] Dialog leftovers (hints, helpers, primaries) no longer read as 90s internals
- [x] Remaining keyboard gaps in the shell are closed
- [x] Live/offline status leftovers are Sunday on display surfaces
- [x] Composer/action chrome leftovers are Sunday
- [x] CAST/bodycam crop and comic-page placement unchanged
- [ ] `zig fmt`, `zig build test`, `zig build -Dtarget=x86_64-linux`, `git diff --check` pass
- [ ] Committed render-ui previews refreshed if pixels change

## Verification

- Focused gate: `zig build test`
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
