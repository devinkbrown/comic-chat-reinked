# Task contract: CC-20260825-18

## Outcome

PR #13 pastes CLIPBOARD on Shift+Insert / XF86Paste as typed keys, types
Ukrainian/Belarusian/Serbian/Macedonian extra Cyrillic without an IME, and
passes `notify-send --icon=applications-internet`. No chrome, renderer,
AT-SPI, full IME, dedicated drop Event, Wayland taskbar urgency,
`_NET_WM_SYNC_REQUEST`, outgoing source DnD, or Wayland→X11 fallback. Both
Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/xkb.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `src/platform/services.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `794a3ba`

## Evidence and constraints

- Existing behavior: middle-click pastes PRIMARY as typed keys; Insert is
  `Key.other` so chrome ignores Shift+Insert; extra Cyrillic `0x06a1`–
  `0x06af` (Ukrainian i/yi/ie, ghe-with-upturn, Serbian/Macedonian/Belarusian)
  falls through; `notify-send` has urgency but no icon.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no outgoing source DnD.

## Acceptance criteria

- [ ] Shift+Insert and XF86Paste paste CLIPBOARD as typed keys on X11 and
      Wayland
- [ ] Extra Cyrillic X11 keysyms and matching XKB names type without an IME
- [ ] `notify-send` includes `--icon=applications-internet`
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: extra-Cyrillic / paste-key helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
