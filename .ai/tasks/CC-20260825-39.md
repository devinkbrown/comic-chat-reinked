# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/x11.zig`.

## Objective

When an X11 window maps under the pointer without a prior EnterNotify, seed hover from QueryPointer so the first click is not targeting stale `(-1,-1)` coordinates.

## In scope

1. MapNotify QueryPointer when `pointer_inside` is false.
2. If the pointer is inside the mapped window, mark inside and queue the same enter sequence as EnterNotify.

## Out of scope

- UI chrome and the comic renderer
- Wayland taskbar urgency, outgoing source DnD
- KeymapNotify, ISO-8859-10/14/16, KOI8-U
- Full IME editor, AT-SPI/UIA

## Owned paths

- `src/platform/x11.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [x] `queryPointerReplyInside` is true only for same-screen in-bounds win coords
- [x] MapNotify seeds hover when QueryPointer says the pointer is inside
- [x] `zig build test` green; both Linux targets still build

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- Focused gate: QueryPointer-inside unit tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/x11.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 540 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `61483ae`.
- Evidence: green local gates on `61483ae`.
- Residual risk: KeymapNotify, ISO-8859-10/14/16, and KOI8-U stay optional.
- Recommendation: `accept`
