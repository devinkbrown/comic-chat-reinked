# Task contract: CC-20260824-04

## Outcome

Anna HD / Color females render complete in CAST, Character gallery,
bodycam, and strip close-ups: no right-edge wrap sliver, no waist-cut
landscape mugshot, no legs-only strip panels from a padded 240×280 card.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/figure.zig`, `src/comic/original_figure.zig`
  (generated-card silhouette only), `src/comic/source_strip_test.zig`,
  `src/client/view.zig` (test only), `src/main.zig` (`render-chrome`
  paper flatten), this contract.
- Read-only dependencies: pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684`, `tools/package_generated_avb.py`
- Forbidden paths: `src/client/ui.zig` restyle, balloon planner rewrite,
  legacy golden-hash refresh without an upstream routine
- Base commit: `0269a1d`

## Evidence and constraints

- Existing behavior: Anna HD's 240×280 card has two ink column-runs
  (19–111 and 177–224). The union bbox was 206×189 and looked like a
  right-edge clip plus wrap sliver. Strip `GetDimInfo` used `ydim/2` of
  the padded 280-tall card, so close-ups could read as feet.
- Source/reference evidence: `GetDimInfo` `ydim/2` is for authored simple
  avatars. Generated 240×280 cards are not Microsoft AVBs; chrome and
  generated-card layout must contain-fit the opaque silhouette.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*`;
  source goldens stay unless an upstream routine justifies a change.

## Acceptance criteria

- [x] Anna HD / Color female chrome portrait and bodycam are the largest
      ink run, not a half-width union with a wrap sliver.
- [x] Generated standing chrome portraits keep the full silhouette so
      gallery contain-fit is not a waist-cut landscape band.
- [x] Color/HD strip layout/draw crop the padded card to that silhouette
      before `ydim/2`.
- [x] Legacy 1-bit goldens unchanged.
- [x] `zig build test` green; `zig build` succeeds.

## Verification

- Focused gate: `zig build test -- --test-filter "Anna HD|female|chrome|title star|source page"`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/figure.zig`, `src/comic/original_figure.zig`,
  `src/comic/source_strip_test.zig`, `src/client/view.zig`, `src/main.zig`,
  this contract.
- Commands and outcomes:
  - `zig build test --summary all` — 484 passed, 1 skipped
  - `zig build` — linux/amd64 success
  - `git diff --check` — clean
- Evidence: raw Anna HD card ink runs 19–111 and 177–224; after the
  largest-run crop, chrome dumps are 93×189 with no sliver. Color strip
  close-up is head-and-shoulders, not purple heels.
- Residual risk: generated HD/Color AVB pixels themselves still contain
  the wrap sliver; we ignore it rather than restitch. Authored simple
  avatars are unchanged. Balloon fill still paints after bodies.
- Recommendation: `accept`
