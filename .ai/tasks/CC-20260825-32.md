# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/x11.zig`.

## Objective

Honor the server's `GetModifierMapping` so Caps Lock, NumLock, Mode_switch, AltGr, and Super follow the real modifier bits instead of conventional Shift/Lock/Control/Mod1–5 positions. Refresh on MappingNotify request 0.

## In scope

1. Fetch `GetModifierMapping` after the keymap and parse keycodes through the current `GetKeyboardMapping` table.
2. Use those masks for keypad NumLock, Caps Lock, Mode_switch group 2, AltGr Level3, and KeyPress modifier bits.
3. Refresh the map on MappingNotify `MappingModifier` (request 0) and after a keyboard mapping refresh, without dropping queued events.

## Out of scope

- UI chrome and the comic renderer
- KeymapNotify restore of held keys
- Full IME editor, AT-SPI/UIA, outgoing source DnD
- COMPOUND_TEXT 94ⁿ CJK and `ext-data-control`

## Owned paths

- `src/platform/x11.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [x] Caps_Lock on Mod3 selects the Lock mask `0x20`
- [x] Num_Lock on a non-Mod2 slot still selects keypad digits
- [x] MappingNotify request 0 refreshes modifier masks
- [x] Conventional maps still match Shift=1, Lock=2, Control=4, Mod2=0x10
- [x] `zig build test` green; both Linux targets still build

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- `zig build test` — 537 passed, 1 skipped
- `zig build -Dtarget=x86_64-linux` — success
- `zig build -Dtarget=aarch64-linux` — success
- `zig build linux` — success
- `zig build -Dtarget=x86_64-windows` — success
- `git diff --check` — clean
- X11 smoke — window stayed up until timeout (exit 124)

## Handoff

- Files changed: `src/platform/x11.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 537 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `ac0338b`.
- Evidence: green local gates on `ac0338b`.
- Residual risk: KeymapNotify remains optional; ext-data-control and
  COMPOUND_TEXT 94ⁿ CJK stay optional.
- Recommendation: `accept`
