# Task contract: CC-20260824-07

## Outcome

Finish remaining PR #11 chrome leftovers: persist layout
outside Settings, readable connection failures, empty-dialog
helpers, and complete room/member menu hints. CAST crop unchanged.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/view.zig`, `src/client/dialogs.zig`,
  `src/main.zig` (settings persist + connection status + render-ui),
  committed previews
- Forbidden paths: CAST/bodycam blit crop, `src/comic/*`, `src/platform/*`
- Base commit: `e1dcef7`

## Evidence and constraints

- Settings persist is numeric; Vermillion/Cobalt chrome already maps to 0
- View menu, toolbar, column stepper, and comics_view did not save layout
- Connection failure status used raw Zig error names
- CAST blit stays `52×52` at `cell.y + 6`

## Acceptance criteria

- [x] View/toolbar/stepper/comics_view layout writes `ui_*` preferences
- [x] Connection failures stay failed-tone and drop raw error names
- [x] Empty recent/favorites/features have complete helper copy
- [x] Room/member menus have per-item hints; wire-gated when offline
- [x] CAST/bodycam crop unchanged
- [x] Tests green; committed preview PNGs refreshed

## Verification

- Focused gate: `zig build test` — 465 pass, 1 skip + 12 main
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`

## Handoff

- Files changed: `src/client/view.zig`, `src/main.zig`, committed previews
- Residual risk: none on this chrome lane
- Recommendation: `accept`
