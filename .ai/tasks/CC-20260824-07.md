# Task contract: CC-20260824-07

## Outcome

Generated Color/HD characters use independent local color (Anna: peach
skin, red tube top, black skirt). Generated standing females stay fully
visible in chrome and on the comic page — never a purple wash and never
legs-only.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `tools/package_generated_avb.py` (preserve local color),
  `src/assets/generated/*-color-hd-v*.avb` (rebuilt Color packages),
  `src/comic/original_layout.zig` (skip zoom for generated standing
  figures), `src/comic/strip.zig` / `src/comic/figure.zig` (flag +
  tests), this contract.
- Read-only dependencies: pose-sheets-v1 (already locally painted),
  pinned microsoft/comic-chat `c7df00f60bc8e9fdef413f139e61f7c37e024684`
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `ebb2e81`

## Evidence and constraints

- Existing behavior: `colorize_pose` extracts one hue from the Color
  reference and tints every fill pixel. Pose-sheets-v1 Anna already has
  peach skin + red top. Source `LayoutAvatars` zooms around the ground
  line without updating `tops[]`; on a standing generated woman that
  crops to feet.
- Source/reference evidence: testdata goldens require
  `strip.avatarByName("anna")` and default `strip.render` field
  backdrop. Layout goldens require the pre-zoom top for unflagged bodies.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*`;
  source goldens stay unless an upstream routine justifies a change.

## Acceptance criteria

- [x] Color AVBs keep independent local colors. Anna Color has peach
      skin and a red top, not purple skin.
- [x] Packager does not apply a single-hue wash to poses that already
      have local color.
- [x] Generated standing figures skip the source zoom-in branch so a
      continuation panel cannot become legs-only. Testdata zoom goldens
      unchanged.
- [x] CAST / gallery / bodycam / `render-chrome anna` still show the
      full standing silhouette.
- [x] `zig build test` green; source goldens unchanged.
- [x] strip-color, ui-conversation, ui-cast, ui-character, chrome Anna
      refreshed.

## Verification

- Focused gate: Anna local-color test + generated-standing no-legs test
  + source page / title / whisper goldens
- Aggregate gate: compiled module + main tests
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `tools/package_generated_avb.py`, Color AVBs +
  `COLOR_AVB_SHA256SUMS.txt` + generated README, `src/comic/figure.zig`,
  `src/comic/original_layout.zig`, `src/comic/strip.zig`,
  `src/client/view.zig` (gallery height only), `src/main.zig` (wrap
  preview), this contract.
- Commands and outcomes: `zig fmt` owned Zig; `zig build test` 492
  pass / 1 skip; `git diff --check` clean; wrap / conversation / CAST /
  character / chrome Anna previews refreshed.
- Evidence: wrap continuation panels (`...CLEARER NOW.`) now show
  head+torso Anna, not legs-only. Cause was balloon-uppercase leftovers
  selecting a shouting/kick pose. Color/HD pose text stays on the
  original line. Dest clamp keeps generated standing rects inside the
  2300-unit panel. Anna Color chrome is peach + red, 93×189 standing
  card.
- Residual risk: testdata 1-bit `anna` stays the Microsoft mugshot;
  Color sticker AA halo on rooms; comic panels still sit the dest on
  the ground line so feet can kiss the bezel (head+torso is the
  accepted fallback). Gallery height uses leftover dialog space, not a
  `ui.zig` restyle.
- Recommendation: `accept`
