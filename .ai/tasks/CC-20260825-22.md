# Task contract: CC-20260825-22

## Outcome

PR #13 stops CLIPBOARD from serving PRIMARY-owned local text, clears hover
when the pointer leaves, decodes invalid UTF-8 / ISO-8859 paste, and
installs HiDPI window icons. No chrome, renderer, AT-SPI, full IME,
dedicated drop Event, Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`,
outgoing source DnD, ext-data-control, or 94ⁿ CJK COMPOUND_TEXT. Both
Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `5b43701`

## Evidence and constraints

- Existing behavior: CLIPBOARD paste can return PRIMARY-owned copy text;
  pointer leave never moves, so hover sticks; Latin-1 clipboard bytes are
  kept as invalid UTF-8; `_NET_WM_ICON` is only 16/32.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; PRIMARY stays on middle-click only.

## Acceptance criteria

- [x] CLIPBOARD local text is used only when we own CLIPBOARD
- [x] Pointer leave emits a move outside the window; EnterNotify/DnD motion move
- [x] Invalid UTF-8 clipboard bytes decode as Latin-1
- [x] Receive-only ISO-8859-1/15 MIME and COMPOUND_TEXT ISO-8859-2 decode
- [x] X11 icons include 64/128; Wayland icon includes 64@2
- [x] `zig build test` plus both Linux targets and `zig build linux`

## Verification

- Focused gate: clipboard / icon / MIME helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 535 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `e63a2ff`.
- Evidence: green local gates on `e63a2ff`.
- Residual risk: ext-data-control and COMPOUND_TEXT 94ⁿ CJK remain optional;
  outgoing source DnD, full IME, AT-SPI, dedicated drop Event, Wayland
  taskbar urgency, and `_NET_WM_SYNC_REQUEST` stay skipped.
- Recommendation: `accept`
