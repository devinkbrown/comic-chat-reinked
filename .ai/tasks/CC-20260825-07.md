# Task contract: CC-20260825-07

## Outcome

PR #13 closes more session leftovers: X11 property reads no longer drop
queued events, clipboard prefers the owner's TARGETS list, and Central
European keysyms type without an IME. No UI/renderer, no AT-SPI, no full
IME editor, no Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/xkb.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `f236a85`

## Evidence and constraints

- Existing behavior: GetProperty during the event loop uses `readReply`
  and discards events; paste tries every MIME and can stall; Polish /
  Hungarian / Czech keysyms are `.other`.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [ ] Event-loop GetProperty / GetKeyboardMapping stash queued events
- [ ] Clipboard paste prefers the highest-ranked offered TARGETS atom
- [ ] Central European named and Latin-2 X11 keysyms produce characters
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: TARGETS rank / Latin-2 / keysym tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`
