# Task contract: CC-20260825-07

## Outcome

Close remaining Color/HD product-path leftovers that still hurt:
halo, dest under balloon, tail aim, room paper bleed, and a
pose-PNG edge that still packages. Do not move Microsoft goldens
or reopen the Anna/Kevin/Denise standing local-color crop.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_figure.zig`, `src/comic/figure.zig`,
  `src/comic/strip.zig`, `src/comic/original_balloon.zig`,
  `tools/package_generated_avb.py` / generated Color-HD assets only
  if a packaged pose-PNG edge is measured, this contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh,
  Anna/Kevin/Denise standing-crop reopen
- Base commit: `d6211e4`

## Evidence and constraints

- Existing behavior: Color dests paint after balloons; keyed copy
  coverage-blends ink; tails retarget after dest nudge; packager
  used min/max y of the widest column-run, so a disconnected
  heel-row sliver still packaged.
- Invariants: shipped strip stays on `original_*`; goldens stay;
  testdata 1-bit `anna` and Comic Neue stay by design.

## Acceptance criteria

- [x] Measured leftovers that still hurt on Color/HD product path
      are closed without golden drift.
- [x] Anna/Kevin/Denise stay full standing local-color.
- [x] `zig build test` green. Proof on Color product path.

## Verification

- Focused gate: leftover metrics + goldens + chrome standing
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: packager `largest_ink_bbox` keeps the tallest
  y-band (2-row hole). Rebuilt Color/HD AVBs that still packaged a
  wrap sliver. Anna Color/HD and Tiki Color v2 hashes unchanged.
- Commands and outcomes: `zig build test` 503 pass / 1 skip.
  Live wrap/tall Color pages: no dest-under-balloon, tails on dest,
  no sticker halo. Goldens stay.
- Evidence: Denise/Kevin pose-00 bbox drops 43–53px of bottom
  sliver; chrome standing tests still pass.
- Residual risk: testdata 1-bit `anna` stays for goldens. Comic Neue
  is the documented SIL-OFL Comic Sans substitute. Right-edge wrap
  slivers in a separate column-run were already ignored.
- Recommendation: `accept`
