# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/event.zig`, `src/platform/x11.zig`, `src/platform/services.zig`, `src/platform/wayland.zig`.

## Objective

Keep a drag that enters the X11 window as a held primary (or secondary) button, and decode receive-only KOI8-R paste from Russian X11 / GTK sources.

## In scope

1. On EnterNotify, if Button1 or Button3 is already down and that button is not currently tracked as held, emit `.down` then queue the enter hover move. Do not synthesize middle-down (that would surprise-paste PRIMARY). Wayland `wl_pointer.enter` has no button state — skip.
2. Receive-only KOI8-R: MIME `text/plain;charset=KOI8-R` / `koi8-r` / `koi8r` plus the bare ICCCM `KOI8-R` target that xterm offers. Distinctive decode: `0xE1` → `А`.

## Out of scope

- UI chrome and the comic renderer
- Synthesizing middle-down on enter
- Wayland pointer-enter button restore
- Full IME editor, AT-SPI/UIA, outgoing source DnD
- COMPOUND_TEXT 94ⁿ CJK, ext-data-control, ISO-8859-10/14/16

## Owned paths

- `src/platform/event.zig`
- `src/platform/x11.zig`
- `src/platform/services.zig`
- `src/platform/wayland.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [x] EnterNotify with Button1Mask and `!held_primary` emits `.down` then queues the hover move
- [x] EnterNotify with only Button2Mask stays a move (no middle-down)
- [x] KOI8-R `0xE1` decodes to `А`; `isLatinPlainMime` accepts KOI8-R MIME and the bare atom name
- [x] `zig build test` green; both Linux targets still build

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- `zig build test` — 538 passed, 1 skipped
- `zig build -Dtarget=x86_64-linux` — success
- `zig build -Dtarget=aarch64-linux` — success
- `zig build linux` — success
- `zig build -Dtarget=x86_64-windows` — success
- `git diff --check` — clean
- X11 smoke — window stayed up until timeout (exit 124)

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/event.zig`, `src/platform/x11.zig`,
  `src/platform/services.zig`, `src/platform/wayland.zig`, `docs/WORKERS.md`,
  `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 538 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `1a6a212`.
- Evidence: green local gates on `1a6a212`.
- Residual risk: KeymapNotify remains optional; ext-data-control and
  COMPOUND_TEXT 94ⁿ CJK stay optional. Wayland pointer-enter has no button
  state so drag-into-window on Wayland still depends on the compositor.
- Recommendation: `accept`
