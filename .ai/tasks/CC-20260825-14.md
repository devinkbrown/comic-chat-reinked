# Task contract: CC-20260825-14

## Outcome

PR #13 accepts GNOME/Mozilla file drop MIME, pastes PRIMARY via desktop
helpers when the native primary protocol is missing, skips hidden/suspended
presents, and tags notify-send with a normal urgency hint. No chrome,
renderer, AT-SPI, full IME, dedicated drop Event, Wayland taskbar urgency,
`_NET_WM_SYNC_REQUEST`, outgoing source DnD, or Wayland→X11 fallback.
Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `f951cca`

## Evidence and constraints

- Existing behavior: drops only recognize `text/uri-list`; Wayland
  middle-click PRIMARY is silent without `zwp_primary_selection_v1`;
  `present()` still uploads while iconic/obscured/suspended; notify-send
  has no urgency hint.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no outgoing source DnD.

## Acceptance criteria

- [x] `x-special/gnome-copied-files` / `text/x-moz-url` / `application/x-moz-file` yield a local path
- [x] Wayland middle-click PRIMARY falls back to `wl-paste --primary`
- [x] X11 skips `present()` while hidden or fully obscured and exposes on MapNotify
- [x] Wayland skips `present()` while suspended
- [x] `notify-send` includes `--urgency=normal`
- [x] `zig build test` plus both Linux targets

## Verification

- Focused gate: desktop-file MIME helper plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test`, both Linux targets,
  `zig build linux`, `x86_64-windows`, `git diff --check`, native `zig build`,
  X11 smoke exit 124
- Evidence: HEAD `799e64a`; desktop-file MIME tests; PRIMARY helper argv;
  hidden/suspended `present()` early returns; `notify-send --urgency=normal`
- Residual risk: outgoing source DnD, full IME, AT-SPI, dedicated drop Event,
  Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST` remain skipped
- Recommendation: `accept`
