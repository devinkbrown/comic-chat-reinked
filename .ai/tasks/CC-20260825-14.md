# Task contract: CC-20260825-14

## Outcome

Finish remaining leftover 90s shell chrome on PR #11: IRCX/NICK/MOTD/internal
labels, dialog completeness, keyboard leftovers, and live/offline status, without
restyling CAST/bodycam crop or comic-page placement.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `src/client/shell.zig` (focus/keyboard only), `src/main.zig` (user-facing dialog notices / status display copy only)
- Read-only dependencies: stored `setConnectionFailure` format; CAST/bodycam blit; comic-page placement
- Forbidden paths: `src/comic/original_*`, `src/comic/strip.zig`, `src/platform/*`, CAST/bodycam crop, comic-page placement, CI, packaging
- Base commit: `dcdf367`

## Evidence and constraints

- Existing behavior: client menus/dialogs already Sunday-ized IRCX titles; main.zig notices and some field/protocol labels may still say IRCX/NICK/MOTD
- Source/reference evidence: Ink Sunday chrome around the 1:1 page
- Invariants from `docs/WORKERS.md`: no SDL; no second heuristic layout; no golden raster hash refresh without source evidence

## Acceptance criteria

- [x] Remaining user-facing IRCX/NICK/MOTD/internal labels are Sunday
- [x] Dialog leftovers (notices, helpers, hints) no longer read as 90s internals
- [x] Remaining keyboard gaps in the shell are closed
- [x] Live/offline status leftovers are Sunday on display surfaces
- [x] CAST/bodycam crop and comic-page placement unchanged
- [x] Stored `setConnectionFailure` strings stay
- [x] `zig fmt`, `zig build test`, `zig build -Dtarget=x86_64-linux`, `git diff --check` pass
- [x] Committed render-ui previews refreshed if pixels change

## Verification

- Focused gate: `zig build test`
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
