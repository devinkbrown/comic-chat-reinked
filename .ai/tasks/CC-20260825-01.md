# Task contract: CC-20260825-01

## Outcome

Every Color/HD default-cast character is a full standing silhouette
from a correctly split 3×2 sheet (no mid-thigh tiles). Rooms keep
real local color. Close live-page leftovers (face-covering balloons
only if goldens stay; ignored wrap slivers) without moving Microsoft
goldens. Proof shots of Anna plus two other Color characters.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `assets/generated/avatar-pose-sheets-v1/*` (re-split
  only when a sheet/cell mismatch is measured), Color/HD AVBs +
  checksums for those characters, `src/comic/figure.zig` (wrap
  sliver crop), `src/comic/original_figure.zig` / balloon path only
  if goldens stay, `src/main.zig` preview helpers if needed, this
  contract.
- Read-only dependencies: pose-sheets-v1/v2, pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684`
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `b037eda`

## Evidence and constraints

- Existing behavior: Anna was the only measured 3×2 mismatch
  (1019×1543 sheet vs 509×514 tiles). Other v1 poses matched their
  sheet cells. Confirm with ink-height / wrap-sliver / heel-cut
  checks, not size equality alone.
- Source/reference evidence: balloons paint after bodies in
  `CUnitPanel::Draw`. Changing that for testdata moves goldens.
- Invariants from `docs/WORKERS.md`: shipped strip stays on
  `original_*`; source goldens stay unless an upstream routine
  justifies a change.

## Acceptance criteria

- [x] Every Color/HD default-cast member is either a correct 3×2
      cell or re-split from its sheet; no mid-thigh tiles remain.
- [x] Color rooms keep wood / plants / sky (no one-hue wash).
- [x] Live-page leftovers closed when possible without golden drift.
- [x] `strip.avatarByName("anna")` testdata mapping unchanged.
- [x] `zig build test` green; source goldens unchanged.
- [x] Proof shots: Anna plus at least two other Color characters
      (chrome, strip, client).

## Verification

- Focused gate: cast audit + Anna/other Color chrome/strip tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: packager largest-run crop; all Color/HD AVBs
  rebuilt; dest headroom for generated standing figures; chrome
  keeps narrow standing silhouettes; room + cast tests.
- Commands and outcomes: `zig build test` 495 pass / 1 skip.
  Only Anna had a 3×2 size mismatch (already re-split). Other
  v1 poses match their sheet cells.
- Evidence: Anna chrome 86×253; Kevin 96×256; Denise 73×256.
  Wrap panel shows Anna's face under the balloon. Cafe/apartment
  have wood + green + sky bins.
- Residual risk: a 3-line balloon can still overlap a tall dest.
  Edge slivers remain in some pose PNGs; they are no longer
  packaged. Testdata `anna` stays 1-bit.
- Recommendation: `accept`
