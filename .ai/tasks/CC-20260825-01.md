# Task contract: CC-20260825-01

## Outcome

PR #13 gains bounded XCompose locale-file support, extra X11/Wayland
WM/clipboard polish, and Linux packaging helpers. Both Linux architectures
still compile. No UI/renderer work.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/compose_file.zig`, `src/platform/xkb.zig`,
  `src/platform/x11.zig`, `src/platform/wayland.zig`, `packaging/`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Read-only dependencies: `src/platform/services.zig`, `src/main.zig`
  event-loop contract
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `4e6fcf7`

## Evidence and constraints

- Existing behavior: built-in dead-key / Multi_key composer is live; locale
  Compose files are not loaded. X11 already has ICCCM CLIPBOARD/PRIMARY,
  INCR, UTF8_STRING/STRING, and WM size hints. Wayland already offers
  text/plain and primary selection.
- Source/reference evidence: XCompose text format; EWMH `_NET_WM_ICON_NAME`
  / `_NET_WM_USER_TIME`; xdg-shell `set_min_size` / `set_max_size`; ICCCM
  `TEXT`.
- Invariants from `docs/WORKERS.md`: no SDL; no unpinned TLS; nonempty
  `WAYLAND_DISPLAY` selects Wayland with no X11 fallback; no UI/renderer
  edits; compose tables stay bounded.

## Acceptance criteria

- [x] Bounded XCompose parser loads `XCOMPOSEFILE`, `~/.XCompose`, or `%L`
- [x] X11 and Wayland feed locale keysym names into `Compose` when a table
      is present, otherwise keep the built-in pairs
- [x] X11 serves/requests `TEXT`, sets `_NET_WM_ICON_NAME` and
      `_NET_WM_USER_TIME`
- [x] Wayland advertises xdg min/max size and accepts extra text MIME types
- [x] Packaging adds a Linux install helper and AppStream metainfo
- [x] `zig build test` plus both Linux targets compile; no UI/renderer edits

## Verification

- Focused gate: compose parser tests plus both Linux target compiles
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux` and `aarch64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes` (commit, push, update PR #13)
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/compose_file.zig`, `src/platform/xkb.zig`,
  `src/platform/x11.zig`, `src/platform/wayland.zig`, `packaging/`,
  `docs/WORKERS.md`, `README.md`, completeness/structure docs
- Commands and outcomes:
  - `zig build test` — success
  - `zig build -Dtarget=x86_64-linux` — success
  - `zig build -Dtarget=aarch64-linux` — success
  - `zig build linux` — amd64 + arm64 artifacts
  - `zig build -Dtarget=x86_64-windows` — success
  - `git diff --check` — clean
- Evidence: bounded XCompose load path wired into both Linux backends;
  TEXT / EWMH user-time / xdg min-max / AppStream install helper
- Residual risk: full IME editor and AT-SPI remain out of scope; Compose
  sequences longer than 4 keys or larger than the file/entry caps are
  ignored
- Recommendation: `accept`
