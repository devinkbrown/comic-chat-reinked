# Task contract: CC-20260825-06

## Outcome

PR #13 closes more usable-desktop leftovers: held modifiers on keyboard
enter, X11 iconic/`WM_STATE`, screen-mm HiDPI fallback, Wayland
`preferred_buffer_scale`, and UTF-16 clipboard MIME. No UI/renderer, no
AT-SPI, no full IME editor, no Wayland→X11 fallback. Both Linux arches
still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `1b9577f`

## Evidence and constraints

- Existing behavior: Wayland keyboard enter ignores the held-keys array;
  X11 iconic is Unmap-only; HiDPI has no screen-mm fallback; compositor
  is bound at v4 so `preferred_buffer_scale` never arrives; Wayland does
  not accept UTF-16 offers.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [ ] Wayland restores Shift/Ctrl/Alt/Super from the keyboard-enter keys array
- [ ] X11 tracks ICCCM `WM_STATE` / `WM_CHANGE_STATE` iconic
- [ ] Screen millimeter size and `preferred_buffer_scale` feed integer HiDPI
- [ ] Wayland accepts UTF-16 text MIME without advertising it as a source
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: held-keys / scale-mm / MIME / WM_STATE tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`
