# Task contract: CC-20260825-06

## Outcome

Color/HD live pages keep full standing local-color figures. Close a
1px synthesized sticker edge on colored rooms if it still shows, plus
any leftover wrap-sliver, crop, or Color balloon-tail aim, without
moving Microsoft goldens.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_figure.zig` (sticker edge),
  `src/comic/figure.zig` / `src/comic/strip.zig` / balloon tail only
  if a leftover is measured, this contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `addfd49`

## Evidence and constraints

- Existing behavior: Color synthesizes a MERGEPAINT sticker from
  chromatic ink. Area-sampled fringe can still bleach a 1px edge.
  Testdata uses authored aura and source draw order.
- Invariants: shipped strip stays on `original_*`; goldens stay.

## Acceptance criteria

- [ ] Color sticker does not bleach a 1px paper ring on cafe/apartment.
- [ ] Color/HD chrome stays standing; no wrap-sliver crop.
- [ ] Color tails stay on the post-nudge dest. Testdata goldens stay.
- [ ] `zig build test` green. Proof shots: Anna plus two other Color
      characters.

## Verification

- Focused gate: sticker + wrap/chrome + goldens
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Recommendation: `accept`
