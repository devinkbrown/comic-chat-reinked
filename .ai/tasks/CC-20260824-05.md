# Task contract: CC-20260824-05

## Outcome

Close remaining real 1:1 leftovers on PR #12: Color/HD close-ups that
still read as feet, cheap wrap-sliver / crop-edge loss, and balloon
overlap that can be fixed without moving Microsoft goldens.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/figure.zig`, `src/comic/original_figure.zig`
  (generated-card crop only; return type of `takePaddedSimpleCard`),
  `src/comic/source_strip_test.zig`, this contract.
- Read-only dependencies: pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684`
- Forbidden paths: `src/client/ui.zig` restyle, balloon planner rewrite,
  zoom `tops[]` rewrite, legacy golden-hash refresh without an upstream
  routine
- Base commit: `639f8d7`

## Evidence and constraints

- Existing behavior: generated cards crop to the widest ink run; testdata
  simple/complex avatars stay on decoded DIB dimensions. Balloon fill
  still paints after bodies (source order). Packaged HD/Color DIBs still
  contain a right-edge wrap sliver.
- Source/reference evidence: `GetDimInfo` `ydim/2` and `CUnitPanel::Draw`
  body-then-elements order are required for testdata goldens.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*`;
  source goldens stay unless an upstream routine justifies a change.

## Acceptance criteria

- [x] Color/HD close-ups are not feet-only when that can be done without
      moving testdata goldens. Wave 4 already crops before `ydim/2`.
      Updating `tops[]` after zoom would move testdata goldens; left
      source-faithful.
- [x] Wrap-sliver / crop-edge loss reduced if cheap (pad or ignore
      sliver); do not restitch a DIB unless the decode bug is proven.
      Largest ink run now grows by the packager's 12px paper pad and
      stops before the next ink column-run.
- [x] Balloon overlap only if testdata goldens stay put. Draw order
      unchanged (`CUnitPanel::Draw`). Generated dummy `face.x` `(120,60)`
      remaps to the head-band ink centroid so tails do not aim at the
      right-hand paper.
- [x] `zig build test` green; source goldens unchanged.

## Verification

- Focused gate: `zig build test -- --test-filter "Anna HD|female|scaled title|source page|whisper"`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/figure.zig`, this contract.
- Commands and outcomes:
  - `zig build test --summary all` — 485 passed, 1 skipped
  - `zig build` — linux/amd64 success
  - `git diff --check` — clean
- Evidence: generated-card inventory shows only Anna HD/Color have a
  wrap sliver (19–112 and 177–225). After pad, assembled Color/HD cards
  keep ≥4px of paper around the ink run; authored Jordan `face.x` is
  unchanged. Chrome dumps stay 93×189 (white pad keys away). Color
  `render-strip` close-ups are waist-up, not heels. Source goldens
  `0xa5f59c9cf450aa75`, `0xa62f64877e1db6d0` / `0x57ccc49b4d4dacf1`,
  `0x5aaa64fe213ff6cd` unchanged.
- Residual risk: packaged AVB pixels still contain the wrap sliver; we
  pad around the largest run and ignore the sliver rather than restitch.
  Balloon white fill still paints after bodies (source order).
- Recommendation: `accept`
