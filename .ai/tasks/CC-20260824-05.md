# Task contract: CC-20260824-05

## Outcome

Finish PR #11 desktop chrome as a complete product: menus, settings,
composer, empty states, and connection. Do not restyle CAST/bodycam crop.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, committed `ui-*-preview.png`
- Forbidden paths: CAST/bodycam blit crop, `src/comic/*`, `src/platform/*`
- Base commit: `f76e691`

## Evidence and constraints

- Existing behavior: Ink Sunday chrome around a 1:1 source page
- Invariants from `docs/WORKERS.md`: no SDL; no second heuristic layout;
  CAST/bodycam crop stays `52×52` / existing body-camera blit
- Settings persist `"Light studio"` / `"Dark studio"` and accent `"Cobalt"`;
  label `"Color theme"` stays pinned

## Acceptance criteria

- [ ] Menus show hints, disabled plates, and overflow; popups stay reachable
- [ ] Settings prefill from live appearance/shell; accent hint names Cobalt
- [ ] Composer and connection chrome stay readable when empty or offline
- [ ] Empty page/CAST copy reacts to connection tone
- [ ] No CAST/bodycam crop or blit-size changes
- [ ] Tests green; committed preview PNGs refreshed

## Verification

- Focused gate: `zig build test` plus `render-ui` variants
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept`
