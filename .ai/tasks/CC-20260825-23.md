# Task contract: CC-20260825-23

## Outcome

PR #13 refreshes HiDPI window icons when integer scale changes, accepts
X11 ISO-8859-2 paste MIME, and decodes COMPOUND_TEXT ISO-8859-15
(`ESC - b`). No chrome, renderer, AT-SPI, full IME, dedicated drop Event,
Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`, outgoing source DnD,
ext-data-control, or 94ⁿ CJK COMPOUND_TEXT. Both Linux arches still build.

## Route

- Score: `1 + 1 + 2 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `ba53390`

## Evidence and constraints

- Existing behavior: `_NET_WM_ICON` and `xdg_toplevel_icon_v1` are installed
  once at map; scale changes only refresh the cursor. X11 has ISO-8859-1/15
  MIME but not ISO-8859-2. COMPOUND_TEXT `ESC - B` is Latin-2; Latin-9
  (`ESC - b`) still falls through to Latin-1.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; PRIMARY stays on middle-click only.

## Acceptance criteria

- [x] X11 `applyScale` reinstalls `_NET_WM_ICON`
- [x] Wayland integer scale change reinstalls the 32@1 / 64@2 icon
- [x] X11 receive-only ISO-8859-2 MIME decodes
- [x] COMPOUND_TEXT `ESC - b` decodes ISO-8859-15
- [x] `zig build test` plus both Linux targets and `zig build linux`

## Verification

- Focused gate: icon-scale / charset helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 535 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `2e92b7e`.
- Evidence: green local gates on `e3b5936` / `2e92b7e`.
- Residual risk: ext-data-control and COMPOUND_TEXT 94ⁿ CJK remain optional.
- Recommendation: `accept`
