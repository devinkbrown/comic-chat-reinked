# Task contract: CC-20260825-04

## Outcome

PR #13 closes another usable-desktop slice: visible HiDPI cursors, a
window icon, X11 urgency on notify, and cheap pointer leftovers. No
UI/renderer, no AT-SPI, no full IME editor, no Wayland→X11 fallback.
Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `49a44dc`

## Evidence and constraints

- Existing behavior: no client cursor (Wayland often hides the pointer);
  window title/icon-name only; notify-send has no taskbar urgency;
  framebuffer scale exists but cursor size does not follow it.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [ ] X11 and Wayland show a default pointer cursor, HiDPI-sized when possible
- [ ] X11 sets `_NET_WM_ICON`; Wayland sets xdg-toplevel-icon when advertised
- [ ] X11 notify sets urgency / `_NET_WM_STATE_DEMANDS_ATTENTION` and FocusIn clears it
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: cursor/icon/size unit tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`
