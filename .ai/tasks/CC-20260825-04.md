# Task contract: CC-20260825-04

## Outcome

Color/HD live pages keep full standing local-color figures. A balloon
that is still taller than the dest gap after docking at the top no
longer covers a Color face. Testdata keeps Microsoft body-then-balloon
order. Packaged AVBs stay standing. Rooms stay multi-hue.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/strip.zig` (Color-only dest-after-balloon
  draw), `src/comic/original_balloon.zig` only if a Color lift helper
  needs a still-overlapping signal, this contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `f95e542`

## Evidence and constraints

- Existing behavior: Color dests nudge and balloons lift. A cloud
  taller than `(panel_top - dest.y)` still docks at the top and can
  paint over the face because `CUnitPanel::Draw` draws balloons last.
- Invariants: testdata dests do not use `keep_recognizable` and must
  keep source draw order.

## Acceptance criteria

- [x] Color standing dests paint after balloons so a panel-height
      cloud cannot cover the face.
- [x] Testdata / Microsoft goldens stay on body-then-balloon order.
- [x] Wrap / Color chrome stay standing local-color; rooms multi-hue.
- [x] `zig build test` green. Proof shots: Anna plus two other Color
      characters.

## Verification

- Focused gate: goldens + Color wrap/tall-balloon/chrome tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: Color dests draw after balloons; testdata keeps
  Microsoft body-then-balloon order.
- Commands and outcomes: `zig build test` 501 pass / 1 skip.
- Evidence: Anna/Kevin/Denise chrome 86×253 / 96×256 / 73×256.
  Wrap p1 face stays clear under a top-docked 3-line balloon.
- Residual risk: testdata 1-bit `anna` stays for goldens. Pose PNG
  edge pixels remain; they are not packaged. Comic Neue is the
  documented SIL-OFL Comic Sans substitute.
- Recommendation: `accept`
