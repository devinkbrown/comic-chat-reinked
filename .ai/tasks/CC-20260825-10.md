# Task contract: CC-20260825-10

## Outcome

PR #13 closes more usable-desktop leftovers: Wayland CapsLock from the
compositor lock mask, Arabic and Latin-9 keysyms, and xdg-shell
`wm_capabilities` / `configure_bounds` when the compositor advertises
them. No UI/renderer, no AT-SPI, no full IME editor, no Wayland→X11
fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/xkb.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Read-only dependencies: `src/platform/services.zig`, `src/platform/event.zig`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `8072231`

## Evidence and constraints

- Existing behavior: Wayland CapsLock is only a local evdev toggle, so a
  lock already held at keyboard enter is missed; Arabic and Latin-9
  keysyms are `.other`; xdg-shell is bound at v1 so bounds/capabilities
  events never arrive.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits; no XIM/AT-SPI.

## Acceptance criteria

- [ ] Wayland applies conventional Lock (bit 1) from `wl_keyboard.modifiers`
- [ ] Arabic named and X11 keysyms produce characters
- [ ] Latin-9 OE/oe/Ydiaeresis produce characters
- [ ] xdg-shell binds v5+ when advertised and records bounds/capabilities
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: CapsLock lock-mask / Arabic / Latin-9 / xdg capability tests
- Aggregate gate: `zig build test`
- Build gate: `zig build` plus `x86_64-linux` and `aarch64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
