# Task contract: CC-20260825-08

## Outcome

PR #13 closes more usable-desktop leftovers: clipboard newline
normalization, RANDR / output-mm HiDPI refresh, Greek keysyms, and
key-repeat after keyboard enter. No UI/renderer, no AT-SPI, no full IME
editor, no Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/xkb.zig`,
  `src/platform/x11.zig`, `src/platform/wayland.zig`, `docs/WORKERS.md`,
  `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `100d49e`

## Evidence and constraints

- Existing behavior: paste keeps CR/LF; X11 scale ignores RANDR screen
  changes; Wayland output geometry is unused; Greek keysyms are `.other`;
  keyboard enter does not arm key-repeat.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [x] Clipboard text normalizes CR/LF to LF
- [x] X11 RANDR ScreenChangeNotify and Wayland output mm refresh integer scale
- [x] Greek named and X11 keysyms produce characters
- [x] Wayland keyboard enter arms repeat for a held non-modifier key
- [x] `zig build test` plus both Linux targets

## Handoff

- Files changed: `src/platform/services.zig`, `src/platform/xkb.zig`,
  `src/platform/x11.zig`, `src/platform/wayland.zig`, `docs/WORKERS.md`,
  `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes:
  - `zig build test` — success
  - `zig build -Dtarget=x86_64-linux` — success
  - `zig build -Dtarget=aarch64-linux` — success
  - `zig build linux` — success
  - `zig build -Dtarget=x86_64-windows` — success
  - `git diff --check` — clean
  - X11 smoke — exit 124
- Residual risk: no full IME editor, no AT-SPI, no Wayland taskbar urgency
- Recommendation: `accept`

## Verification

- Focused gate: newline / Greek / RANDR / held-key tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`
