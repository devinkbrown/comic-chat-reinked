# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/x11.zig`.

## Objective

Middle-click PRIMARY paste must not use local CLIPBOARD bytes after another client has taken PRIMARY, and ConvertSelection must stash more than 32 events so INCR paste does not drop clicks or keys.

## In scope

1. `readPrimaryNative` uses local text only while `owns_primary` is true (not `owns_clipboard`).
2. Raise the ConvertSelection / INCR event stash cap from 32 to 256.

## Out of scope

- UI chrome and the comic renderer
- Changing unlabeled invalid-UTF-8 fallback
- KeymapNotify, ISO-8859-10/14/16, KOI8-U
- Full IME editor, AT-SPI/UIA, outgoing source DnD
- COMPOUND_TEXT 94ⁿ CJK, ext-data-control

## Owned paths

- `src/platform/x11.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [ ] Local PRIMARY text is used only when `owns_primary`
- [ ] Owning CLIPBOARD alone does not satisfy PRIMARY local-text
- [ ] ConvertSelection stash cap is 256
- [ ] `zig build test` green; both Linux targets still build

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- Focused gate: PRIMARY local-text helper test
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
