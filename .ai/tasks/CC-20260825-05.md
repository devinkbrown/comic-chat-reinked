# Task contract: CC-20260825-05

## Outcome

Color/HD live pages keep full standing local-color figures. Close
AA halo, leftover wrap-sliver pixels, Color balloon tail aim after
dest nudge, and any crop that still clips a Color/HD figure, without
moving Microsoft goldens.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_figure.zig` (sticker AA),
  `src/comic/figure.zig` (paper-ink sliver), `src/comic/original_balloon.zig`
  (Color tail retarget), `src/comic/strip.zig` (apply retarget), this
  contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `42e13f1`

## Evidence and constraints

- Existing behavior: sticker treats all-channels >= 242 as paper;
  mid-gray AA (230-241) MERGEPAINTs a pale halo. Balloon tails use
  speaker_box from the pre-nudge dest. Runtime crop uses the widest
  ink column-run.
- Invariants: testdata dests/auras stay on the Microsoft path.

## Acceptance criteria

- [ ] Near-gray AA is paper on Color sticker, not a halo.
- [ ] Color tails aim at the post-nudge dest. Testdata goldens stay.
- [ ] Packaged Color/HD chrome stays standing; no wrap-sliver crop.
- [ ] `zig build test` green. Proof shots: Anna plus two other Color
      characters.

## Verification

- Focused gate: sticker + sliver + wrap/chrome + goldens
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Recommendation: `accept`
