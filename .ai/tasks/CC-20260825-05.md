# Task contract: CC-20260825-05

## Outcome

PR #13 closes another usable-desktop slice: layout-group input, compose
reset, physical HiDPI size hints, cursor refresh, clipboard BOM/UTF-16,
DnD actions, server-side decorations, and hidden/tiled window state.
No UI/renderer, no AT-SPI, no full IME editor, no Wayland→X11 fallback.
Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/xkb.zig`,
  `src/platform/x11.zig`, `src/platform/wayland.zig`, `docs/WORKERS.md`,
  `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `4bd561c`

## Evidence and constraints

- Existing behavior: compose can stick after focus loss; X11 size hints
  are logical pixels; Wayland drops may omit `set_actions`; no SSD
  request; UTF-16/BOM paste is raw; fallback cursor does not follow
  scale changes.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [x] Compose resets on X11 FocusOut; X11/Wayland honor keyboard group 2
- [x] X11 WM size hints and cursor refresh use physical/HiDPI scale
- [x] Clipboard strips UTF-8 BOM and decodes UTF-16; Wayland DnD sets copy
- [x] Wayland requests server-side decorations when advertised
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: group/BOM/path/state tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`
