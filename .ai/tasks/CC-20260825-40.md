# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/x11.zig`.

## Objective

Unmap / minimize / shade must clear hover so MapNotify QueryPointer can seed on restore. Today `pointer_inside` stays true across hidden, so CC-39 never runs after iconify.

## In scope

1. Becoming hidden (UnmapNotify, `_NET_WM_STATE_HIDDEN` / `SHADED`, ICCCM `WM_STATE` / `WM_CHANGE_STATE`) emits the leave sequence when the pointer was inside.
2. Becoming visible seeds hover via QueryPointer when the pointer is not already inside.

## Out of scope

- UI chrome and the comic renderer
- Wayland taskbar urgency, outgoing source DnD
- KeymapNotify, ISO-8859-10/14/16, KOI8-U
- Full IME editor, AT-SPI/UIA
- `file://` `.local` / extra charset tables

## Owned paths

- `src/platform/x11.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [x] `hiddenPointerAction` is leave when becoming hidden while inside
- [x] `hiddenPointerAction` is seed when becoming visible while not inside
- [ ] `zig build test` green; both Linux targets still build

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- Focused gate: hidden-pointer action unit tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
