# Task contract: CC-20260825-11

## Outcome

PR #13 closes more first-class Linux desktop leftovers: X11 ConvertSelection
timestamps, TARGETS-first XDND, middle-click PRIMARY paste as typed keys,
receive-only `text/html`, and Wayland text-input refresh on keyboard
enter. No UI/renderer, no AT-SPI, no full IME editor, no dedicated drop
Event, no Wayland taskbar urgency, no `_NET_WM_SYNC_REQUEST`, no
Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `ade7324`

## Evidence and constraints

- Existing behavior: ConvertSelection uses CurrentTime 0; XDND ignores the
  owner's TARGETS list and drop timestamp; middle-click does not paste
  PRIMARY; HTML-only clipboard offers are dropped; Wayland keyboard enter
  does not refresh text-input.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no chrome/renderer edits.

## Acceptance criteria

- [x] X11 ConvertSelection sends a user timestamp, not CurrentTime 0
- [x] XDND prefers TARGETS and uses the drop timestamp
- [x] Middle-click pastes PRIMARY as typed keys on X11 and Wayland
- [x] Receive-only `text/html` strips tags to plain text
- [x] Wayland keyboard enter refreshes text-input
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: timestamp / HTML / middle-paste helpers plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
