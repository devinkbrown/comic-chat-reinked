# Task contract: CC-20260825-11

## Outcome

Finish remaining Ink Sunday chrome on PR #11: tooltips, focus rings, disabled
states, dialog completeness, overflow rooms, composer/status leftovers, and any
90s-shell copy still showing. Do not restyle CAST/bodycam crop or comic-page
placement.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `src/main.zig` (settings label / title / render-ui only), committed preview PNGs, `.ai/tasks/CC-20260825-11.md`
- Read-only dependencies: `src/client/geometry.zig`, `src/comic/strip.zig`, `src/client/hit_test.zig`
- Forbidden paths: `src/comic/original_*`, `src/comic/strip.zig` (except confirming 1:1 unchanged), `src/platform/*`, CI, packaging
- Base commit: `090646b`

## Evidence and constraints

- Existing behavior: Ink Sunday chrome around a 1:1 page is already in place through `090646b`.
- Source/reference evidence: renderer owns CAST blit and page placement; this pass is chrome only.
- Invariants from `docs/WORKERS.md`: no SDL; no second heuristic layout; no golden raster hash refresh without source evidence.

## Acceptance criteria

- [x] Toolbar, overflow, status, disabled, and dialog chrome have printed-Sunday tooltips or hints where a user would stall.
- [x] Focus rings exist for menus, dialogs, overflow, and fields without painting over composer top ink (`say.y+1, x=2`).
- [x] Disabled room/member/moderation chrome looks disabled and is skipped by keyboard.
- [x] Remaining dialogs have named primaries, helpers, and notices where empty/error.
- [x] Overflow rooms (`...`) have hover, tooltip, keyboard, and focus.
- [x] Composer/status leftover 90s-shell copy is gone.
- [x] CAST blit and page placement unchanged.
- [x] `zig fmt`, `zig build test`, `zig build -Dtarget=x86_64-linux`, `git diff --check` green.

## Verification

- Focused gate: pixel/copy tests for composer ink, focus, disabled skip, overflow, dialogs
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `.ai/tasks/CC-20260825-11.md`, committed preview PNGs
- Commands and outcomes: `zig fmt`, `zig build test` green, `zig build -Dtarget=x86_64-linux` green, `git diff --check` clean
- Evidence: overflow jump/hover/keyboard tests; toolbar skip when wire is dead; dialog titles/primaries pinned; CAST blit still `52×52` at `cell.y + 6`
- Residual risk: none for this chrome pass
- Recommendation: `accept`
