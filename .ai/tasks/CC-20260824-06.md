# Task contract: CC-20260824-06

## Outcome

The live client, `render-strip`, and `render-ui` show a vividly colored
cast and colored rooms by default. Original 1-bit Microsoft packs remain
only for goldens and the explicit Original family.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/session.zig` (bare-name Color default),
  `src/client/view.zig` (chrome remap, comic avatar remap, backdrop
  fallback), `src/client/dialogs.zig` (picker order), `src/main.zig`
  (render-strip / render-ui defaults), tests that assert those defaults,
  this contract.
- Read-only dependencies: generated Color AVB/BGB packs; pinned
  microsoft/comic-chat `c7df00f60bc8e9fdef413f139e61f7c37e024684`
- Forbidden paths: `src/client/ui.zig` restyle, `strip.avatarByName`
  testdata mapping for bare `anna`, golden-hash refresh, Anna crop revert
- Base commit: `af771e7`

## Evidence and constraints

- Existing behavior: `avatarForNick` already assigns Color names; transcript
  backdrop defaults to `color apartment`. Chrome remaps bare names to HD
  ink. `setAvatar(..., "anna")` and `render-ui conversation` still select
  1-bit testdata. `render-strip` defaults to the source/field golden path.
- Source/reference evidence: testdata goldens require
  `strip.avatarByName("anna")` and `strip.render` field backdrop to stay
  1-bit.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*`;
  source goldens stay unless an upstream routine justifies a change.

## Acceptance criteria

- [x] Live/session bare names (`anna`) resolve to Color counterparts.
      Explicit `original` / `hd` / `color` suffixes stay as selected.
- [x] CAST / bodycam / gallery chrome remaps bare names to Color.
- [x] Live comic page remaps leftover bare names to Color before
      `strip.renderWithOptions`.
- [x] Character picker leads with Color; backdrop picker leads with Color
      rooms. Fallback unknown backdrop is a Color room, not `field`.
- [x] `render-strip` default and `render-ui conversation` show Color cast
      and a Color room. `render-strip source` stays 1-bit for goldens.
- [x] Anna/female crop and MERGEPAINT Color sticker stay.
- [x] `zig build test` green; source goldens unchanged.

## Verification

- Focused gate: `zig build test -- --test-filter "source page|scaled title|whisper|character gallery|conversation archive|transcript applies|avatarForNick|Color female"`
- Aggregate gate: compiled module test binary
  `.zig-cache/o/7ba58fc57d5c1eaf40b9799d4538a116/test` — 474 passed, 1 skipped
- Main tests: `zig build test` run-test 12 pass
- Combined: 486 passed, 1 skipped
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/session.zig`, `src/client/view.zig`,
  `src/client/dialogs.zig`, `src/client/files.zig`, `src/main.zig`,
  this contract.
- Commands and outcomes:
  - module tests 474 passed / 1 skipped
  - main tests 12 passed
  - `zig build` linux/amd64 success
  - `git diff --check` clean
  - previews: `/opt/cursor/artifacts/screenshots/strip-color.png`,
    `strip-source.png`, `ui-conversation.png`, `ui-cast.png`,
    `ui-character.png`, `ui-background.png`,
    `chrome-anna-default-portrait.png`
- Evidence: `render-chrome anna` SHA equals `anna color`; `anna hd` differs.
  `render-strip color` uses color apartment + Color speakers.
  `render-strip source` remains testdata + field.
  Source goldens unchanged (`0xa5f59c9cf450aa75`,
  `0xa62f64877e1db6d0` / `0x57ccc49b4d4dacf1`, `0x5aaa64fe213ff6cd`).
- Residual risk: Color MERGEPAINT sticker still paints AA fringe as white
  on chromatic rooms. Packaged wrap sliver still in pixels. Testdata
  1-bit names stay mapped in `strip.avatarByName` for goldens.
- Recommendation: `accept`
