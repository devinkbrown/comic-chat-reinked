# Task contract: CC-20260824-06

## Outcome

Finish remaining PR #11 product chrome: connection/setup, empty/error
states, menu wire gating, Vermillion accent label, composer. CAST crop
unchanged.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`,
  `src/main.zig` (settings label prefill + render-ui surfaces only), committed previews
- Read-only dependencies: `src/client/geometry.zig`, `src/comic/strip.zig`
- Forbidden paths: CAST/bodycam blit crop, `src/comic/*`, `src/platform/*`, CI, packaging
- Base commit: `a56ad29`

## Evidence and constraints

- Existing behavior: Ink Sunday chrome around a 1:1 source page
- Accent persist is numeric `ui_accent` 0/1/2; dialog string is chrome
- Settings label `"Color theme"` and `"Light studio"` / `"Dark studio"` stay pinned
- CAST blit stays `cell.x + @divTrunc(cell.w - 52, 2), cell.y + 6, 52, 52`
- Bodycam blit stays `rect.x + 12, rect.y + 30, rect.w - 24, body_h - 30`
- Composer top hairline `say.y+1, x=2` stays ink; no full editor focus ring

## Acceptance criteria

- [x] Settings choice shows Vermillion; leftover Cobalt apply still maps to accent 0
- [x] Setup/servers/features/password have WIRE chrome and a default TLS/wire hint
- [x] Room/member menus and member context disable until the wire is live
- [x] Empty/error/composer/status copy covers failed, offline, waiting, and live
- [x] CAST/bodycam crop and blit size unchanged
- [x] Tests green; committed preview PNGs refreshed

## Verification

- Focused gate: `zig build test` plus `render-ui` variants
- Aggregate gate: `zig build test` — 464 pass, 1 skip + 12 main
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`,
  `src/main.zig`, committed `ui-*-preview.png`
- Commands and outcomes: `zig build test` 464/1 skip + 12 main; linux build; hygiene clean
- Evidence: Vermillion chrome; Cobalt apply stays 0; CAST 52×52 blit unchanged
- Residual risk: none on this chrome lane
- Recommendation: `accept`
