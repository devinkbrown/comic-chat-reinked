# Task contract: CC-20260825-17

## Outcome

PR #13 pastes receive-only `text/rtf` as plain text, treats `text/x-uri-list`
like `text/uri-list`, and re-watches the XSETTINGS owner after the settings
daemon restarts. DestroyNotify from that owner must not close the chat
window. No chrome, renderer, AT-SPI, full IME, dedicated drop Event,
Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`, outgoing source DnD, or
Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `d0be409`

## Evidence and constraints

- Existing behavior: Office/rich-text paste offers `text/rtf` only and
  fails; some drops use `text/x-uri-list`; XSETTINGS owner is queried once
  at open; DestroyNotify is treated as close for any window.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no outgoing source DnD.

## Acceptance criteria

- [x] `text/rtf` / `application/rtf` strip to plain text
- [x] `text/x-uri-list` yields a local `file:` path
- [x] XSETTINGS owner is re-queried on scale refresh and DestroyNotify
- [x] DestroyNotify for the XSETTINGS window does not close the client
- [x] `zig build test` plus both Linux targets

## Verification

- Focused gate: RTF/uri-list helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test`, both Linux targets,
  `zig build linux`, `x86_64-windows`, `git diff --check`, native `zig build`,
  X11 smoke
- Evidence: HEAD `d8d4fbd`; RTF/uri-list helper tests; XSETTINGS DestroyNotify
  ignores the settings window
- Residual risk: outgoing activation tokens, Shift+Insert paste, extra
  Cyrillic, full IME, AT-SPI, dedicated drop Event, Wayland taskbar urgency,
  `_NET_WM_SYNC_REQUEST`, outgoing source DnD remain skipped
- Recommendation: `accept`
