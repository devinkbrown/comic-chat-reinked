# Task contract: CC-20260825-19

## Outcome

PR #13 passes an outgoing activation / startup token to `xdg-open` so a
file or URL opened from the chat can take focus. Wayland requests a real
`xdg_activation_v1` token when advertised; X11 supplies a unique
`DESKTOP_STARTUP_ID`. No chrome, renderer, AT-SPI, full IME, dedicated
drop Event, Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`, outgoing
source DnD, or Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/wayland.zig`,
  `src/platform/x11.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `2980414`

## Evidence and constraints

- Existing behavior: inbound `XDG_ACTIVATION_TOKEN` is consumed for our
  window; `xdg-open` is spawned with no token, so the opened app often
  stays behind the chat.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; inbound token is still startup-only, not notify/urgency.

## Acceptance criteria

- [ ] `xdg-open` receives `XDG_ACTIVATION_TOKEN` and `DESKTOP_STARTUP_ID`
      when a token is available
- [ ] Wayland requests `xdg_activation_v1.get_activation_token` when bound
- [ ] X11 generates a unique startup id for the child
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: activation env helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
