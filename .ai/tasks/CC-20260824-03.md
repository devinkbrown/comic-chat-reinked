# Task contract: CC-20260824-03

## Outcome

Title-panel starring icons for generated HD/Color use a readable mugshot
instead of a smashed 64×64 `AK_ICON`, and Color DIBs get a MERGEPAINT white
sticker so SRCAND does not AND colored ink into the backdrop (tails no longer
read as cutting that ink). CAST/bodycam stay on the chrome mugshot/body path.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/figure.zig`, `src/comic/original_figure.zig`,
  `src/comic/strip.zig` (title star blit only), this contract.
- Read-only dependencies: pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684` (`panel.cpp:1437` CBodyUnary
  `m_icon`, `bodycam.cpp:589-609` MERGEPAINT then SRCAND)
- Forbidden paths: `src/client/ui.zig` restyle, balloon planner rewrite,
  legacy golden-hash refresh without an upstream routine
- Base commit: `1bf4446`

## Evidence and constraints

- Existing behavior: title panel `blitImage`s `decodeIcon`; generated HD
  icons are `resize((64,64))`. Color simple poses have no authored aura, so
  SRCAND of chromatic pixels ANDs into the backdrop.
- Source/reference evidence: title stars are `CBodyUnary` of `m_icon` drawn
  with `CBodySingle::DrawBody` (nimbus + SRCAND). 1-bit aura MERGEPAINT
  paints the white sticker; Color packages must synthesize that sticker.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*`;
  source goldens (`0xa5f59c9cf450aa75` etc.) stay unless an upstream routine
  justifies a change.

## Acceptance criteria

- [x] Generated 64×64 simple-avatar title stars use `chromePortrait` when
      the pose card is much larger than the icon (HD smash / Color pancake).
      Authored icons, including Jordan’s 64×64 simple mugshot, stay
      `decodeIcon`. Legacy complex icons stay `decodeIcon`.
- [x] Color/HD simple poses without an authored aura MERGEPAINT a paper
      sticker before SRCAND so chromatic ink survives a non-white dest.
- [x] Legacy 1-bit goldens unchanged.
- [x] CAST/bodycam keep the previous mugshot/body helpers.
- [x] `zig build test` green; `zig build` succeeds.

## Verification

- Focused gate: `zig build test -- --test-filter "title star|paper sticker|chrome"`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/figure.zig`, `src/comic/original_figure.zig`,
  `src/comic/strip.zig`, `.ai/tasks/CC-20260824-03.md`
- Commands and outcomes:
  - `zig build test --summary all` — 482 passed, 1 skipped
  - `zig build` — linux/amd64 success
  - `git diff --check` — clean
- Evidence: `panel.cpp:1437` CBodyUnary `m_icon`; `bodycam.cpp:602-609`
  MERGEPAINT then SRCAND; 1×1 red-on-green fixture is `0xffff0000` not
  `red & green`. Color title crop
  `/opt/cursor/artifacts/screenshots/strip-color-title.png` shows
  head-and-shoulders Anna/Kevin Color stars, not smashed 64×64 bodies.
- Residual risk: balloon white fill still paints after bodies (source
  order). The sticker stops SRCAND from eating Color ink; a tail that
  overlaps a face still covers it, same as 1-bit. Simple-avatar strip
  zoom still uses `headHeight = ydim/2`, so a tall Color close-up can
  read as a feet crop — that is source GetDimInfo, not CAST chrome.
- Recommendation: `accept`
