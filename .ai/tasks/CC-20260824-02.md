# Task contract: CC-20260824-02

## Outcome

CAST pane thumbnails, Character gallery portraits, mood/bodycam figures, and
roster icons blit the same AVB art Microsoft Comic Chat uses for member icons
versus the body camera: mugshot vs full body, SRCAND paper matte, no nimbus.

## Route

- Score: `1 + 1 + 2 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/figure.zig` (chrome portrait/body helpers),
  surgical `src/client/view.zig` blit/integration, `src/main.zig` cast
  preview surface, this contract.
- Read-only dependencies: pinned microsoft/comic-chat
  `c7df00f60bc8e9fdef413f139e61f7c37e024684` (`avatar.cpp:63`,
  `avatar.h:251` GetIconPose, `bodycam.cpp:464-609` DrawBody/GetBodyBox),
  `docs/WORKERS.md`
- Forbidden paths: desktop chrome rewrite (`src/client/ui.zig` restyle),
  strip planner/balloon redesign, golden-hash refreshes without an upstream
  routine, network/TLS
- Base commit: `cursor/renderer-source-parity-1b3d` @ `5832f19`

## Evidence and constraints

- Existing behavior: chrome was stretching unkeyed `AK_ICON` cards
  (including smashed HD 64×64 full bodies) with bottom-align; bodycam
  assembled a white sticker then keyed it after the head paper had already
  erased overlapping torso ink.
- Source/reference evidence: `CAvatarX::GetIconPose` is `AK_ICON`;
  `CBodyCam::DrawBody(..., FALSE)` is full-body contain-fit, bottom-centered,
  no aura; `CBodySingle`/`CBodyDouble` drawings use `SRCAND` (white leaves
  dest); simple `headHeight = ydim/2`.
- Invariants from `docs/WORKERS.md`: shipped strip stays on `original_*` +
  `strip.zig`; do not invent a second layout path; no UI chrome redesign.

## Acceptance criteria

- [x] Member/CAST/Character portraits are heads (authored icon or silhouette
      mugshot), never a smashed full-body 64×64 stretch.
- [x] Generated 64×64 simple-avatar icons use a 3/5 silhouette mugshot;
      authored simple icons keep GetIconPose.
- [x] Bodycam is the trimmed full figure, contain-fit, bottom-aligned,
      SRCAND-keyed, no nimbus; head paper does not cut the torso.
- [x] Simple-avatar `headHeight` remains `ydim/2` for strip layout.
- [x] Legacy, Color, and HD bundled avatars all produce visible keyed art.
- [x] Renderer/UI tests stay green; linux/amd64 `zig build` succeeds.
- [x] CAST/character/bodycam previews refreshed on PR #12.

## Verification

- Focused gate: `zig test -Mroot=src/root.zig --test-filter chrome`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/comic/figure.zig`, `src/client/view.zig`,
  `src/main.zig`, `.ai/tasks/CC-20260824-02.md`
- Commands and outcomes:
  - `zig build` — linux/amd64 success
  - `zig build test --summary all` — 480 passed, 1 skipped
  - `git diff --check` — clean
- Evidence: `avatar.h:251` GetIconPose; `bodycam.cpp:499` DrawBody FALSE;
  `avatar.cpp:63` ydim/2 layout-only; Anna HD opaque bbox y=85 made a raw
  ydim/2 crop a 206×55 hair slice; silhouette 3/5 + 5:4 frame yields 142×113.
- Residual risk: title-panel starring still draws authored `AK_ICON`
  (smashed HD) to keep strip goldens; Color DIB nimbus is part of the art.
- Recommendation: `accept`
