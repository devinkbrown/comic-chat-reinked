# Task contract: CC-20260824-02

## Outcome

Polish Ink Sunday chrome (masthead, tool rack, page well, tabs, CAST /
Character / mood, composer, settings, menus, empty and conversation states,
hover / focus, HiDPI strokes) on `cursor/redesign-desktop-ui-7646` without
touching strip renderer algorithms. Refresh committed `render-ui` PNGs.

## Route

- Score: `1 + 1 + 1 + 1 = 4`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths:
  - `src/client/ui.zig`
  - `src/client/view.zig`
  - `src/client/dialogs.zig`
  - `docs/UI_LIBRARY.md`
  - committed `ui-*-preview.png`
- Read-only dependencies:
  - `src/comic/original_*.zig`
  - `src/comic/strip.zig`
  - `src/platform/*`
- Forbidden paths:
  - renderer algorithms, golden raster hashes, protocol/network/backend
- Base commit: `6d4f60b`

## Evidence and constraints

- Existing behavior: Ink Sunday chrome around a 1:1 prefix-planned page.
- Source/reference evidence: printed Sunday-page chrome; strip stays `strip.renderWithOptions`.
- Invariants from `docs/WORKERS.md`: no SDL, no second heuristic layout, no golden hash refresh.

## Acceptance criteria

- [x] Structural chrome strokes are at least two framebuffer pixels.
- [x] Room tabs, composer, settings, menus, and empty/conversation states read as printed Sunday chrome.
- [x] Pixel tests and `zig build test` stay green.
- [x] Committed preview PNGs match the polished chrome.
- [x] Strip blit path is unchanged.

## Verification

- Focused gate: `zig build test` plus `render-ui` variants
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/client/ui.zig`, `src/client/view.zig`, `src/client/dialogs.zig`, `docs/UI_LIBRARY.md`, committed `ui-*-preview.png`
- Commands and outcomes: `zig build test` 460 pass / 1 skip plus 12 main tests; `zig build -Dtarget=x86_64-linux`; `git diff --check` clean
- Evidence: refreshed README previews; 2px `ui.stroke` / `drawInkPlate` / `drawInkRule`; room-tab hover
- Residual risk: Settings still persist `"Cobalt"` for vermillion
- Recommendation: `accept`
