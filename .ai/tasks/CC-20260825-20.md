# Task contract: CC-20260825-20

## Outcome

PR #13 stops CLIPBOARD reads from falling through to PRIMARY, defers a
Wayland copy until a seat serial exists, exposes after leaving the
xdg-shell suspended state, and types NumLock keypad keys on X11 and
Wayland. No chrome, renderer, AT-SPI, full IME, dedicated drop Event,
Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`, outgoing source DnD, or
Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/xkb.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `b58c2b8`

## Evidence and constraints

- Existing behavior: empty CLIPBOARD pastes PRIMARY; Wayland copy before
  the first seat serial fails; unsuspend can skip expose; keypad keys
  ignore NumLock.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; PRIMARY stays on middle-click only.

## Acceptance criteria

- [ ] CLIPBOARD paste does not read PRIMARY
- [ ] Wayland `writeClipboard` stores text and advertises once a serial exists
- [ ] Leaving xdg `suspended` emits expose
- [ ] NumLock selects keypad digits on X11 and Wayland (including evdev fallback)
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: keypad / clipboard helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
