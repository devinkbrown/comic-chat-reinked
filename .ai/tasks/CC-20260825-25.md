# Task contract: CC-20260825-25

## Outcome

X11 ignores grab/ungrab pointer and pointer-only focus so hover and
compose stay stable, wheel button releases do not emit empty clicks, and
ISO-8859-9 paste / COMPOUND_TEXT `ESC - M` decode. No chrome, renderer,
AT-SPI, full IME, dedicated drop Event, Wayland taskbar urgency,
`_NET_WM_SYNC_REQUEST`, outgoing source DnD, ext-data-control, or 94ⁿ CJK.
Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `110fad9`

## Evidence and constraints

- Existing behavior: LeaveNotify/FocusIn/FocusOut fire on NotifyGrab and
  pointer-focus details; wheel ButtonRelease emits `.none` up; ISO-8859-9
  paste has no decoder.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11 fallback.

## Acceptance criteria

- [x] X11 Enter/Leave ignore grab/ungrab modes
- [x] X11 FocusIn/Out ignore grab/ungrab and pointer-focus details
- [x] X11 wheel button releases do not emit pointer up
- [x] Receive-only ISO-8859-9 MIME and COMPOUND_TEXT `ESC - M` decode
- [x] `zig build test` plus both Linux targets and `zig build linux`

## Verification

- Focused gate: notify-mode / ISO-8859-9 helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/services.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 535 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `432c950`.
- Evidence: green local gates on `a3cb4f0` / `432c950`.
- Residual risk: ext-data-control and COMPOUND_TEXT 94ⁿ CJK remain optional.
- Recommendation: `accept`
