# Task contract: CC-20260825-02

## Outcome

PR #13 closes remaining X11/Wayland holes that hurt a live session:
bounded IME leftovers (no full editor), clipboard/WM leftovers, and
accessibility basics that stay property-sized. No UI/renderer. No
Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 2 = 7`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Read-only dependencies: `src/platform/event.zig`, `src/main.zig` loop
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `c1e3bc4`

## Evidence and constraints

- Existing behavior: text-input-v3 commits and suppresses preedit; IME
  candidate window has no cursor rectangle; `last_committed` can eat the
  next key after a mouse-confirmed IME commit. X11 has no `WM_TAKE_FOCUS`,
  no `TIMESTAMP` target, no clipboard-manager handoff, no GTK text MIME
  atoms.
- Invariants from `docs/WORKERS.md`: no SDL; nonempty `WAYLAND_DISPLAY`
  selects Wayland with no X11 fallback; no full IME editor; no AT-SPI bus.

## Acceptance criteria

- [x] Wayland IME sets a bounded cursor rectangle and a multiline content hint
- [x] IME keyboard de-dupe only swallows the confirming keysym, not the next real key
- [x] X11 handles `WM_TAKE_FOCUS` / FocusIn and claims input focus
- [x] X11 clipboard serves `TIMESTAMP` and GTK text MIME; hands off to
      `CLIPBOARD_MANAGER` on close when present
- [x] Bounded a11y/WM: `WM_LOCALE_NAME` and `_NET_WM_ALLOWED_ACTIONS`
- [x] `zig build test` plus both Linux targets; no UI/renderer edits

## Verification

- Focused gate: new IME/clipboard/WM unit tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Build gate: `zig build -Dtarget=x86_64-linux` and `aarch64-linux`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes` (commit, push, update PR #13)
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes:
  - `zig build test` — success
  - `zig build -Dtarget=x86_64-linux` — success
  - `zig build -Dtarget=aarch64-linux` — success
  - `zig build linux` — amd64 + arm64 artifacts
  - `zig build -Dtarget=x86_64-windows` — success
  - `git diff --check` — clean
- Evidence: IME cursor rectangle + confirming-key de-dupe; X11 focus,
  TIMESTAMP/GTK MIME, clipboard-manager handoff, locale/actions
- Residual risk: full IME editor and AT-SPI/UIA provider transport remain
  out of scope
- Recommendation: `accept`
