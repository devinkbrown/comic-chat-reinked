# Task contract: CC-20260825-02

## Outcome

Color/HD live pages keep full standing local-color figures. 3-line
balloons no longer cover faces when that can be done without moving
Microsoft goldens. Remaining wrap-sliver / narrow-chrome crops on
the packaged cast are closed. Rooms stay multi-hue.

## Route

- Score: `2 + 1 + 1 + 1 = 5`
- Primary lane: `balanced`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `source-parity`

## Ownership

- Owned paths: `src/comic/original_layout.zig` (Color-only headroom),
  `src/comic/strip.zig` (optional Color-only balloon clamp),
  `src/comic/figure.zig` (chrome crop / cast tests), packager only
  if a leftover sliver is measured in a packaged card, this contract.
- Forbidden paths: `src/client/ui.zig` restyle, testdata AVB/BGB,
  `strip.avatarByName("anna")` testdata mapping, golden-hash refresh
- Base commit: `efd7aa7`

## Evidence and constraints

- Existing behavior: Color dest headroom is `max(700, unit_height/3)`
  (~766). Short balloons clear the face; 3-line balloons can still
  overlap. `cropHeadPortrait` now keeps standing cards when
  `h > w + 20`. Testdata dests do not use `keep_recognizable`.
- Invariants: shipped strip stays on `original_*`; goldens stay.

## Acceptance criteria

- [x] Color-only dest/balloon change leaves testdata goldens unchanged.
- [x] 3-line / tall wrap balloons do not cover Anna's face when the
      dest still shows a full standing figure.
- [x] Packaged Color/HD chrome cards stay standing (no narrow
      head-band crop); rooms stay multi-hue.
- [x] `zig build test` green. Proof shots: Anna plus two other Color
      characters.

## Verification

- Focused gate: goldens + Color wrap/chrome tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: Color dest headroom 920; `nudgeRecognizableDestBelow`
  after balloon layout; wrap face-peach assert; Color+HD chrome
  standing test.
- Commands and outcomes: `zig build test` 498 pass / 1 skip.
  Wrap p1 shows Anna's face under the 3-line balloon; heels in frame.
- Evidence: Anna/Kevin/Denise chrome 86×253 / 96×256 / 73×256.
  All 22 Color and 22 HD chrome portraits are standing, no sliver.
- Residual risk: testdata dests stay on the Microsoft path. A balloon
  taller than dest min-height can still overlap. Pose PNG edge pixels
  remain; they are not packaged.
- Recommendation: `accept`
