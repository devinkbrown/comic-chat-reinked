# Task contract: CC-20260825-21

## Outcome

PR #13 closes a first-class Linux desktop wave on X11 and Wayland: CLIPBOARD
stays off PRIMARY, Wayland copy waits for a seat serial, unsuspend/unhide/
FocusIn/activate expose, NumLock keypad columns, XKB groups 3–4 plus
Mode_switch, receive-only COMPOUND_TEXT, extra KDE/Mozilla file MIME, and
text-input disable when unfocused. No chrome, renderer, AT-SPI, full IME,
dedicated drop Event, Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`,
outgoing source DnD, or Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/x11.zig`, `src/platform/wayland.zig`,
  `src/platform/xkb.zig`, `src/platform/services.zig`, `docs/WORKERS.md`,
  `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `b58c2b8`

## Evidence and constraints

- Existing behavior: empty CLIPBOARD pastes PRIMARY; Wayland copy before
  the first seat serial fails; unsuspend/unhide can skip expose; keypad
  keys ignore NumLock; tri-layout group 3 uses group 2; xterm/Emacs
  COMPOUND_TEXT is dropped; Dolphin/Firefox extra file MIME is dropped;
  IME can stay enabled after deactivate.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; PRIMARY stays on middle-click only.

## Acceptance criteria

- [x] CLIPBOARD paste does not read PRIMARY
- [x] Wayland `writeClipboard` stores text and advertises once a serial exists
- [x] Leaving xdg `suspended`, X11 hidden, or gaining X11/Wayland focus exposes
- [x] NumLock XOR Shift selects keypad digits on X11 and Wayland
- [x] Wayland XKB groups 3–4 wrap; X11 Mod3 Mode_switch selects group 2
- [x] Receive-only COMPOUND_TEXT decodes to UTF-8
- [x] Extra KDE5 / Mozilla-priv file MIME yields a local path
- [x] Wayland text-input disables when the toplevel is not activated
- [x] `zig build test` plus both Linux targets and `zig build linux`

## Verification

- Focused gate: keypad / group / COMPOUND_TEXT / MIME helper tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/platform/{x11,wayland,xkb,services}.zig`, `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 535/536 pass (1 skip); `x86_64-linux` / `aarch64-linux` / `linux` / `x86_64-windows` compile; `git diff --check` clean
- Evidence: HEAD `089870a` (feature); this docs commit records acceptance
- Residual risk: COMPOUND_TEXT still skips 94^n CJK sets; no ext-data-control; outgoing source DnD still chrome-owned
- Recommendation: `accept`
