# Task contract: CC-20260825-15

## Outcome

PR #13 reads XSETTINGS integer scale, maps X11 XI2 touch to the shared
pointer contract when the device is not already pointer-emulating, and
accepts KDE/Nautilus file-list MIME. No chrome, renderer, AT-SPI, full
IME, dedicated drop Event, Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`,
outgoing source DnD, or Wayland→X11 fallback. Both Linux arches still build.

## Route

- Score: `2 + 1 + 2 + 1 = 6`
- Primary lane: `balanced`
- Required reviewer: `fast`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/platform/services.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `docs/WORKERS.md`, `README.md`,
  `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Forbidden paths: `src/comic/`, `src/render/`, `src/client/` chrome
- Base commit: `cursor/linux-native-backends-0d8a` @ `799e64a`

## Evidence and constraints

- Existing behavior: GNOME XSETTINGS scale is ignored; X11 touchscreens
  that do not emulate a core pointer produce no events; KDE uri-list MIME
  is unrecognized unless `text/uri-list` is also offered.
- Invariants: nonempty `WAYLAND_DISPLAY` selects Wayland with no X11
  fallback; no new Event variant; no outgoing source DnD. GenericEvent
  extra length must be drained so the X11 stream cannot desync.

## Acceptance criteria

- [ ] `Gdk/WindowScalingFactor` / `Xft/DPI` in an XSETTINGS blob yield integer scale
- [ ] XI2 `TouchBegin/Update/End` become pointer down/move/up; emulating-pointer touches are ignored
- [ ] `application/x-kde4-urilist` / `x-special/nautilus-clipboard` yield a local path
- [ ] GenericEvent extra bytes are consumed
- [ ] `zig build test` plus both Linux targets

## Verification

- Focused gate: XSETTINGS parser plus XI2 unit tests plus both Linux compiles
- Aggregate gate: `zig build test`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`
