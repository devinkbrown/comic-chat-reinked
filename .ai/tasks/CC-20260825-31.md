# Task Contract

## Score

- Scope: 2
- Ambiguity: 1
- Risk: 2
- Verification: 1
- Lanes: 1
- Total: 6
- Route: balanced
- Command: `python3 tools/ai_route.py --scope 2 --ambiguity 1 --risk 2 --verification 1 --lanes 1`

## Owner

Linux desktop backends (PR #13): `src/platform/x11.zig`, `src/platform/wayland.zig`, `src/platform/services.zig`, `src/platform/event.zig`.

## Objective

Close remaining first-class X11/Wayland holes that still hurt daily use: release a held pointer button on leave so composer/emotion drag cannot stick, clear urgency when `_NET_WM_STATE_FOCUSED` is gained, and decode commonly offered Baltic/Greek/Turkish/Hebrew/Arabic Windows and ISO-8859-13 paste.

## In scope

1. LeaveNotify / `wl_pointer.leave` emit `.up` for a held button, then `(-1,-1)` hover clear.
2. Clear X11 urgency / `_NET_WM_STATE_DEMANDS_ATTENTION` when `_NET_WM_STATE_FOCUSED` is gained.
3. Decode receive-only ISO-8859-13 and Windows-1253/1254/1255/1256/1257 MIME on X11 and Wayland.

## Out of scope

- UI chrome and the comic renderer
- Full IME editor, AT-SPI/UIA, dedicated drop Event
- Wayland taskbar urgency, `_NET_WM_SYNC_REQUEST`, outgoing source DnD
- COMPOUND_TEXT 94ⁿ CJK, ISO-8859-13 COMPOUND_TEXT designator, and `ext-data-control`
- GetModifierMapping / KeymapNotify

## Owned paths

- `src/platform/event.zig`
- `src/platform/x11.zig`
- `src/platform/wayland.zig`
- `src/platform/services.zig`
- `docs/WORKERS.md`
- `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- `README.md`

## Acceptance criteria

- [x] Pointer leave with a held button emits `.up` then `(-1,-1)`
- [x] Gaining `_NET_WM_STATE_FOCUSED` clears urgency
- [x] ISO-8859-13 paste `Ą` (`0xC0`); windows-1253 `Α` (`0xC1`); windows-1254 `Ğ` (`0xD0`); windows-1255 `א` (`0xE0`); windows-1256 `ا` (`0xC7`); windows-1257 `Ą` (`0xC0`)
- [x] `zig build test` green; both Linux targets still build
- [x] Nonempty `WAYLAND_DISPLAY` still selects Wayland with no X11 fallback

## Gates

- `zig build test`
- `zig build -Dtarget=x86_64-linux`
- `zig build -Dtarget=aarch64-linux`
- `zig build linux`
- `git diff --check`

## Verification

- `zig build test` — 536 passed, 1 skipped
- `zig build -Dtarget=x86_64-linux` — success
- `zig build -Dtarget=aarch64-linux` — success
- `zig build linux` — success
- `zig build -Dtarget=x86_64-windows` — success
- `git diff --check` — clean
- X11 smoke — window stayed up until timeout (exit 124)

## Handoff

- Files changed: `src/platform/event.zig`, `src/platform/x11.zig`,
  `src/platform/wayland.zig`, `src/platform/services.zig`, `src/main.zig`,
  `docs/WORKERS.md`, `README.md`, `docs/PORTABLE_COMPLETENESS_AUDIT.md`
- Commands and outcomes: `zig build test` 536 passed / 1 skipped;
  `x86_64-linux`, `aarch64-linux`, `linux`, `x86_64-windows` compile;
  `git diff --check` clean; X11 smoke timeout 124 on `bd741ec`.
- Evidence: green local gates on `bd741ec`.
- Residual risk: GetModifierMapping / KeymapNotify remain; ext-data-control
  and COMPOUND_TEXT 94ⁿ CJK stay optional.
- Recommendation: `accept`
